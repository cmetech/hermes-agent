# v3.0.3 follow-up: MCP-capable CLI AI tour

Status: deferred and non-normative for Workflow v3.0.3 Revision 7.

Desktop/background execution can run the verified AI showcase when the
server-owned runner and resolved runtime both support request-carried MCP
through the Hermes tool loop. The deterministic CLI showcase runner remains
MCP-incapable and therefore reports the scenario as incompatible. Removing the
obsolete AI confirmation token did not grant capability.

A future CLI tour must use a real MCP-capable runner, classify the actual
runtime, start required package MCP before provider traffic, and own complete
cleanup. It must not special-case the showcase ID, bridge package MCP into
`codex_app_server`, or use the dormant laptop interpretation resource
implicitly.
