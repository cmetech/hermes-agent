# v3.0.3 release review deferrals

Status: non-normative follow-up register for Workflow v3.0.3 Revision 7 and
its adversarial runtime remediation.

No Critical, Important, or implementation Minor remains accepted from the
task-scoped reviews. Task 1.2's exact sanitized log proof and Task 4.7's
exactly-once scheduling proof were closed during Task 5.

The Revision 7 adversarial runtime review's accepted RT-1, RT-2, B-1, RT-3,
RT-4, and B-2 findings are closed by the remediation commits recorded in
`.superpowers/sdd/revision-7-adversarial-remediation-report.md`. The following
items remain deliberately outside that focused repair:

- **RT-5 — fire-time transient classification.** No lane produced a
  reproducible transient trigger. Before changing terminal-versus-retry
  behavior, define an explicit exception taxonomy and a bounded retry contract
  that keeps the inner and outer fire-time paths coherent.
- **RT-6 — full-tree verification under the admission transaction.** Slow-disk
  lock duration is a real trade-off, but moving the hash outside the lock
  reopens the check/use race. Retain the current atomic placement until a
  design preserves exact consume-time identity.
- **B-3 — verify/load/verify byte sandwich.** The low-trigger double-swap case
  needs consume-time verified-byte plumbing. A local ordering tweak would only
  narrow, not close, the boundary; the trigger remains outside the accepted
  local single-operator threat model.
- **SK-RT-02 — re-presented consumed promotion authorization.** The complete
  source path is valid, but no production caller batches the affected
  in-session retry shape; keep as a latent hardening follow-up.
- **SK-CC-A — coordinator-health diagnostic suppression.** The planned
  retryable 503 mapping stays, but a programming-error trigger could be logged
  more precisely. No concrete corrupt-row or parser trigger was demonstrated.
- **SK-CC-B — code-less input snapshot error mapping.** No shipped scenario
  exercises the component-collision trigger; retain as a typed-error
  hardening follow-up.

Disputed or unverified evidence is retained explicitly:

- **CC-2 provenance** remains disputed between Pre-existing and Exposed; the
  underlying stale-poll presentation issue is unchanged either way.
- **RT-6's proposed pre-hash fix** remains rejected until its TOCTOU regression
  is solved.
- End-to-end duplicate terminal-notification delivery across a coordinator
  restart remains unverified. Layered idempotency keys make it non-blocking,
  but a targeted restart-during-delivery probe would improve evidence.
- No second provider/model lane was available for the adversarial review, so
  no cross-provider reviewer consensus is claimed.

The review's non-blocking presentation and maintenance decisions remain:

- **CC-4:** the scheduled existing-join toast deliberately keeps
  "Already running."
- **CC-6:** unknown future `run_support.reason` copy/version-skew behavior.
- **CC-7:** the currently unreachable `assert isinstance` admission guard.
- **SK-CC-C:** stale canonical schedule reuse on retry, retained for
  idempotency stability.
- **SB-02:** approval-node timeout reclassification.
- **SB-3:** exact five-showcase release-membership pins, retained as a sealed
  release inventory despite change-detector tension.

Pre-existing issues stay separate from the branch verdict:

- **CC-1:** several typed admission failures use generic Desktop input copy.
- **CC-2:** an in-flight poll can briefly repaint a mutation-fresh snapshot.
- **CC-3:** non-conflict cancel failures have no user-visible feedback.
- **CC-5:** mapping-shaped failure causes can render as `[object Object]`.
- A non-`RuntimeError` escape from `_execute_claim` can leave a claim until its
  lease expires; ordinary lease recovery remains the self-healing boundary.

The following evidence remains manual-only when an already-authorized,
configured environment is unavailable:

- configured-Gateway structured `tool_calls` round trip for OTTO;
- configured-Gateway structured `tool_calls` round trip for LOOP24;
- Electron laptop-diagnostic approval and rejection;
- Electron ai-extensions execution through the configured Gateway to success;
- Electron Run later → Scheduled → due success;
- Electron cancel-before-fire; and
- Electron approval-gate regression.

These items are release-operator gates, not permission to make automated
external or billable calls. Task 3.4's historical manifest-update sequencing
note cannot be repaired without rewriting history; the final manifest covers
the surface and its checker must pass at the handoff SHA.
