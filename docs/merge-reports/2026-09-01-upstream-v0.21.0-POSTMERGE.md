# POSTMERGE report — upstream v0.21.0 → `base`

## Outcome

The exact upstream v0.21.0 release was merged successfully into `base` after
the approved rehearsal. The real merge reproduces the approved candidate tree,
all merge-critical validation passed against the real merge commit, and all 17
customization manifests now use v0.21.0 as their verified upstream baseline.

This operation did not move the `otto` or `loop24` refs, build a distributable
package, publish a release, push a branch, or modify a remote.

## Immutable lineage

- Pre-merge `base`: `89f2cb6ea9ebb65681cee8b60305ca33e56870c5`.
- Previous verified upstream: v0.20.6,
  `5fc308a70719a83cccdbba4c0e39c23f5a8239d5`.
- Upstream target: v0.21.0,
  `29112bef099274229cadff79cdff7bf7b99c4b77`.
- Release notes:
  <https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.31>.
- Approved rehearsal commit:
  `f9280e3d07cd95f6c7197c43c6cfb30abc70980f`.
- Real ancestry-preserving merge and tested `base`:
  `59de9bfea2f8b8574a181edd598a218c9b8c63cf`.
- Approved and real merge tree:
  `2c1cd6fe736f329cb759608eabeb14b91d1adca5`.
- Audit closure: the `docs(merge): seal upstream v0.21.0` commit containing
  this report, the trial report, and all 17 advanced manifest baselines.

The real merge has pre-merge `base` as its first parent and the exact v0.21.0
release commit as its second parent. Its tree is byte-identical to the approved
rehearsal tree. Moving `origin/main`, which was 55 commits beyond the requested
release during preflight, was not substituted for the pinned target.

## Reconciliation result

The real merge replayed the rehearsal's 30 reviewed content conflicts and kept
the reviewed dispositions. In particular, it preserves the fork's strict API
tool contracts in upstream's extracted run module, authenticated environment
snapshots and secret-safe execution, one-shot tool-choice behavior, provider
routing, plugin/workflow contracts, Kanban attachment handling, and the unified
Desktop browser settings surface.

The complete conflict inventory, silent-overlap review, contract dispositions,
and individual reconciliation findings are preserved in
`2026-09-01-upstream-v0.21.0-TRIAL.md`.

## Exact real-merge verification

All results in this section were obtained against real merge commit
`59de9bfea2f8b8574a181edd598a218c9b8c63cf`.

### Customization and canonical gates

- 17 merged-state ledger reports covered 370 records: 365 executable checks
  passed and five reference-only records were reviewed; zero failures and zero
  flaky-first product failures remained.
- Canonical base gate: 4,362 tests passed across 83 files, with eight explicit
  platform skips and `TESTED_BASE_SHA=59de9bfea2f8b8574a181edd598a218c9b8c63cf`.
- Installed-distribution gate: three tests passed.
- Merge-gate Desktop matrix: 211 tests passed.
- Neutral Desktop brand gate and TypeScript typecheck passed.

### Desktop

- Full Vitest suite: 860 files passed, two platform files skipped; 9,261 tests
  passed and six platform tests skipped.
- Production renderer, Electron main/preload, native dependency staging, and
  `assert-dist-built` passed.
- The unpackaged Electron application completed an isolated fake-boot launch
  from the exact real merge and was stopped intentionally after startup.

### Detached brand verification

The brand refs were not used as mutable workspaces. Both overlays were
generated in detached disposable worktrees at the exact real merge.

- All eight generator emitters passed in write and check mode for OTTO and
  LOOP24.
- Both brand merge gates passed with
  `TESTED_BRAND_SHA=59de9bfea2f8b8574a181edd598a218c9b8c63cf`.
- OTTO passed 141 targeted provider/runtime/skin tests; LOOP24 passed 125
  targeted provider/runtime tests. Both catalogs contained the required 11
  messaging platforms.
- Branded and legacy CLI entry-point version launches passed for both overlays.
- Both branded production Desktop builds and isolated unpackaged Electron
  fake-boot launches passed.
- The first detached LOOP24 build inherited the default OTTO selector because
  the checkout was detached. It was not counted. The build was rerun with
  `OTTO_BRAND=loop24`, and `build/brand.json` was explicitly verified as
  `loop24` before the launch check.

At merge closure, the live refs remained at their pre-operation commits: OTTO
`8ccdd6d6066b2deb08b35237ec940f0922ca92e1` and LOOP24
`6c727b41dc4a411243aab006f1be49775ab7a865`.

## Subsequent v8.0.0 release preparation

The local brand refs were later fast-forwarded through ancestry-preserving
merge commits prepared from release cutoff
`96aaed431dbf1d74a99e3d6a3f9e2d666e767b91`:

- OTTO: `5ae2c1a4ea623c1fc22eddf4918ae1d48f89ebe3`.
- LOOP24: `47c4dc29a813278cb1575352be91f241ca01a1da`.

Both exact commits passed their brand merge gate, generator check, macOS arm64
release build, packaged-artifact inspection, v8.0.0 install-stamp validation,
and isolated packaged-app launch. Release packaging also exposed and fixed a
duplicate `--publish never` argument bug in `run-electron-builder.mjs`; the
regression test and both release-faithful builds proved packaging remains
non-publishing.

At the preparation checkpoint, no branch had been pushed and no GitHub release
or tag existed. After explicit publication approval, `base`, `otto`, and
`loop24` were pushed atomically, and both stable v8.0.0 release workflows
completed successfully:

- OTTO run
  [`33549461693`](https://github.com/cmetech/otto/actions/runs/33549461693)
  published [OTTO Desktop v8.0.0](https://github.com/cmetech/otto/releases/tag/v8.0.0)
  with seven assets. The Windows x64 EXE digest is
  `eae0665a4550eab4b815dd37d9e5e22986449b097638c9547f4f96ca35a8650a`;
  the MSI digest is
  `74a4d2380bf92ec0e152cabc4ca82952c8a0b780b0b40f00f5171aa5a5e3964e`.
- LOOP24 run
  [`33549465538`](https://github.com/cmetech/loop24/actions/runs/33549465538)
  published [LOOP24 Desktop v8.0.0](https://github.com/cmetech/loop24/releases/tag/v8.0.0)
  with seven assets. The Windows x64 EXE digest is
  `296a3f652b060fa947e380cfd6ba4576cc3fe146d44b653668ee3b4291d8cae3`;
  the MSI digest is
  `c66ff48e696c357ee0417a47093262e42cf081bf6099e9dc7c3964b03735e79f`.

The workflow logs prove both platform jobs checked out the exact tested brand
SHA, set product version 8.0.0, built Windows NSIS and MSI installers plus the
macOS packages, uploaded every expected artifact, and completed the stable,
non-draft publication job.

## Environmental observations

A concurrent real-commit workflow-ledger run encountered a native macOS SQLite
bus error while executing `test_scheduling_middleware_e2e.py`, matching the
environmental signature observed during rehearsal. The exact file then passed
all six tests in isolation, and the canonical workflow ledger passed cleanly on
its serial rerun. No product failure reproduced.

## Remaining risks

- The v8.0.0 release workflows passed their Windows and macOS build lanes.
  Explicitly Linux-only checks remain covered by their existing CI lanes.
- The pinned upstream release contains two unusually named mutex fixture files
  under `apps/desktop/'/var/folders/...`. They remain unchanged as part of the
  exact release tree; test-created copies were removed from disposable trees.
- Vite reports the existing future `configLoader: native` compatibility warning
  and `advancedChunks` deprecation during production builds. Neither failed the
  build.
- This merge integrates 911 upstream commits. The ancestry pin, reviewed tree
  equivalence, 17 ledgers, canonical gate, full Desktop suite, and detached
  brand gates reduce the risk but do not replace post-merge platform CI.

## Catalog closure

All 178 entries across the 17 customization manifests now use
`29112bef099274229cadff79cdff7bf7b99c4b77` as
`last_verified_upstream`. The next upstream assessment must begin from this
immutable v0.21.0 baseline and must not re-review v0.20.6 through v0.21.0 or
substitute moving `origin/main` for a requested release.
