# Upstream v0.20.5 pre-merge assessment

Date: 2026-08-21
Destination: literal `base`
Pinned upstream target: `fcbd1076a93841fa88855acce810e342a5b78101`
Previous verified upstream: `36cb5ae5530a75def7df3195e49b7a4aa2add482` (v0.20.0)
Current `base`: `e119b3dd46548866565e29992eca5d4e5208b2ef`
Status: **catalog ready for review; actual merge NO-GO until these controls are committed and the isolated trial is approved**

No upstream merge, commit, push, release, or branch movement was performed for
this assessment. The checkout remains on `base`.

## Scope and why this update matters

The target is the immutable v0.20.5 release commit, not moving `origin/main`.
`pyproject.toml` at the target declares `0.20.5`. `origin/main` is already 57
commits beyond the release and must not be used as the merge source.

| Release increment | Commits | `fix` commits | `feat` commits | Changed paths |
|---|---:|---:|---:|---:|
| v0.20.0 → v0.20.1 | 1,444 | 747 | 203 | 2,093 |
| v0.20.1 → v0.20.2 | 1,008 | 647 | 83 | 1,318 |
| v0.20.2 → v0.20.3 | 258 | 139 | 32 | 467 |
| v0.20.3 → v0.20.4 | 154 | 70 | 32 | 268 |
| v0.20.4 → v0.20.5 | 809 | 353 | 145 | 1,256 |
| **Total** | **3,673** | **1,956** | **495** | **3,962 unique paths** |

The largest fix scopes are Desktop (499), gateway (172), agent (89), CLI
(67), cron (65), update (56), tools (37), relay (32), Kanban (31), sessions
(28), TUI (27), and compression (25). This is not a cosmetic release update.

High-impact upstream fixes in the range include credential scoping and SSH log
redaction, authenticated Desktop downloads, independent memory-store
permissions, plugin security scanning, handoff data-loss prevention, corrupt
backend ownership recovery, recurring-cron recovery, relay read-loop deadlock
removal, gateway timeout/recovery hardening, agent stall guards, and
provider-response fallback fixes. These are generally high or critical to the
fork because they affect security boundaries, data durability, availability,
or the core conversation loop.

## Merge footprint

- Prior non-committing inventory found 236 paths changed by both upstream and
  the fork.
- The aborted/cleaned trial inventory found 100 unmerged paths and 237 conflict
  hunks. No trial worktree or merge state remains.
- The repaired 17-manifest catalog emits 176 ledger rows for this target:
  66 no-overlap rows, 95 same-file rows, and 15 owned-symbol rows.
- Forty-one rows require an explicit human disposition before the real merge.

The textual conflicts are only part of the risk. Same-file and owned-symbol
overlaps that Git merges cleanly can still remove a fork invariant silently.

## Preliminary importance review of the 41 decisions

These are triage priorities, not final dispositions. The isolated trial must
still read the upstream intent, downstream introducing commit, callers, and
motivating tests before selecting an outcome.

| Catalog area | Decisions | Initial importance | Default review posture |
|---|---:|---|---|
| Credential storage | 3 | **Critical** | Union upstream changes with profile authority, migration isolation, atomic publication, and Windows/POSIX privacy guarantees. |
| Tool-choice/API contracts | 4 | **Critical / high** | Preserve request-scoped admission, provider mappings, explicit failures, diagnostics, and native structured output without changing cached prompts. |
| Plugin configuration/capability staging | 6 | **Critical / high** | Preserve ownership, collision rejection, profile setup, Desktop projection, authenticated staging, and vendoring while adopting upstream plugin structure. |
| Workflow orchestration | 13 | **High / medium** | Review sealed runtime, tool admission, language contracts, schema/CLI/Desktop status, regression gates, and the three recent Desktop workflow UX surfaces independently. |
| Fork runtime | 6 | **High / medium** | Preserve profile-owned skills, service permissions, brand identity, session restore, and locked status pills; split convergent behavior from remaining fork deltas. |
| Environment runtime | 2 | **High** | Preserve authenticated shell snapshots and non-POSIX SSH account support across all backends. |
| Python isolation | 2 | **High** | Preserve curated Python environments and import-pure runtime modules. |
| Browser profiles | 1 | **High** | Preserve enrolled-profile launch wiring and vision/requirements behavior. |
| Kanban attachments | 2 | **Medium / high** | Preserve reveal authorization and safe preview behavior while adopting upstream UI/API changes. |
| Test harness | 1 | **Medium** | Preserve resource-aware parallelism, wrapper forwarding, retries, and process-tree cleanup. |
| Desktop test infrastructure | 1 | **Medium** | Preserve handled notification dispatch so browser/jsdom runs cannot fail on an unhandled rejection. |

The five permitted human outcomes are:

- `take-upstream`
- `union-adapt`
- `remove-downstream-as-equivalent`
- `retain-downstream-behavior`
- `blocked-unclear`

`retain-downstream-behavior` means adapt the merged result at the behavior
boundary. It never means omit, cherry-pick around, or revert an upstream
commit. Conflict difficulty, schedule, and disinterest are not evidence.

The known convergent candidate is
`desktop-statusbar-default-and-locked-pills`: upstream commit
`120e465c9741d1fbf8f98dcf85ecb9df7cf09a05` also enables the default-visible
status bar. If the fork test passes against upstream behavior, remove the
redundant downstream default override as upstream-equivalent, but retain the
independent locked version/update-pill and flex-shrink guarantees. All other
decision rows remain unclassified until the trial supplies line-level evidence.

## Skill and catalog repairs completed

- Added the canonical Codex/Claude skill at
  `.agents/skills/otto-upstream-merge/` and replaced the workspace Claude skill
  with a compatibility loader so the procedures cannot drift.
- Pinned v0.20.5 to its immutable release SHA and explicitly rejected moving
  `origin/main` as merge scope.
- Required committed manifests, strict base-blob validation, baseline ancestry
  to the target, external evidence storage, and guarded worktree cleanup.
- Added the evidence-based disposition and importance framework above, including
  the explicit decline path and machine-decision mapping.
- Normalized all 17 manifest baselines to the prior verified v0.20.0 sync SHA.
- Added ledgers for credential storage, environment runtime, fork runtime, and
  tool-choice/API contracts.
- Expanded plugin, test-harness, Python-isolation, Desktop-test, and workflow
  ledgers for changes made since the last upstream merge.
- Removed the obsolete live-system self-test/platform-guard catalog entry that
  the fork intentionally deleted.
- Corrected stale catalog symbols and repaired the Electron node-test split so
  all six `node:test` Electron suites are actually executed.
- Fixed the nested test-harness probes so they behave as ordinary wrapper
  callers under the ledger controller.

## Verification evidence

| Check | Result |
|---|---|
| Canonical Codex skill validation | Pass |
| Workspace Claude compatibility skill validation | Pass |
| All 17 manifests structurally validated | Pass |
| All 17 baselines are ancestors of pinned v0.20.5 | Pass |
| Electron `node:test` group | 49 passed |
| Desktop TypeScript checks | Pass |
| Sixteen complete manifest invariant runs | Pass |
| Workflow manifest run | 234 passed, 4 references, 1 concurrent-scheduling failure |
| Isolated failed workflow test | Pass directly and pass in a serial ledger run |
| Diff whitespace check | Pass |

The workflow failure was caused by running the workflow and plugin manifests
concurrently during this assessment: both executed the same temporary
Git-clone seed test while workflow capability fixtures were active. The
canonical skill intentionally runs manifests serially. The failed test passed
both directly and through the serial ledger controller; it is not a product
failure.

## Readiness and next gate

The catalog changes are intentionally uncommitted for review. Because the
repaired skill trusts only committed merge controls, this is the current and
correct blocker to starting the real v0.20.5 procedure.

After review:

1. Commit only the canonical skill, catalog/test-runner repairs, and this
   assessment to `base`, preserving unrelated user files.
2. Run the skill's strict, serial preflight and isolated v0.20.5 trial from the
   committed `base` SHA.
3. Produce the full 41-row disposition report with test evidence and a
   `GO`, `GO-WITH-FIXES`, or `NO-GO` result.
4. Stop for explicit approval before the real upstream merge and brand
   regeneration.

Publishing, pushing, and creating branded releases remain separate actions and
are not authorized by this assessment.
