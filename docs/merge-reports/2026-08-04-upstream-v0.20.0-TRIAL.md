# TRIAL merge report — upstream v0.20.0 → base   (2026-08-04)   [DRY RUN — nothing landed on base]

Trial branch: `base-trial-36cb5ae55` (kept locally, fully resolved + staged, NOT committed).
All resolutions are also recorded in `.git/rr-cache` (232 entries) for rerere replay.

## Scope
- Range: `b7a05b6b6 (v0.19.0-era merged tip)..36cb5ae55 (origin/main, v0.20.0 release 2026.8.3)`
  — **3301 commits, 4795 files changed**.
- Note: the workflow ledger's recorded baseline is `aaf5691` — 7 commits short of the truly
  merged v0.19.0 candidate `b7a05b6b6`; the v0.19.0 merge never ran `--set-verified-upstream`.

## What happened on the trial branch
- **Conflicts: 83 files** (10-hunk worst case; 4 giant code-move hunks in `hermes_cli/main.py`).
  Zero conflicts remained; all resolved by the bucket rules (details below).
- **Stage-1 gate**: markers ✓ (none) · py-parse ✓ · channel tests 17 ✓ · release-owner greps ✓ (4/4
  cmetech) · `X-Hermes-Session-Token` = 2 ✓ · browser seam greps ✓ (`_session_trusts_url` 6,
  `_snapshot_blocked_url` 3, six guard-forcing sites verified by test class) · browser ledger
  checker exit 0 ✓ · no new upstream console scripts.
- **Silent-revert greps**: all green after resolution (one initial false alarm — an empty stale
  `plugins/model-providers/otto/` dir in the local checkout; removed).
  One REAL silent revert was caught and fixed: the auto-merge produced a **duplicate
  `devDependencies` key in root `package.json`** (upstream's new eslint block silently lost to
  ours; JSON last-key-wins). Hand-unioned + lockfile regenerated via `npm install`.
- **Emitter anchors**: `generate.mjs --neutralize` dry-run — all 8 emitters report clean on the
  merged tree; `check-neutral` guard exit 0. No emitter re-anchoring needed.
- **Desktop build + sweep** (branded, then tree re-neutralized): build exit 0; bundle sweep —
  0 display "Hermes", 0 ALL-CAPS leaks, release-notes URL ✓, `/[\\/]OTTO$/i` uninstall guard ✓,
  `OTTO.app` 2 / `Hermes.app` 0 ✓, gateway de-brand 0 ✓, DiscardLocal ✓.
- **Tests**: ~1,600+ tests run green across the touched surface, including: browser enrolled
  routing + authority boundary (96, UNEDITED) + full browser sweep (614); web_server (177);
  hermes_state (180); transports (103+65+106); process registry + provider-attempt transport
  (63); skills_sync (35); workflow-gate base subset + brand gates (439); update/profile/skin/
  brand-runtime suites. Channel catalog = 11 on BOTH brands; curation 13/2/rename ✓;
  brand.json payload ✓; uninstall identity ✓.

## The dominant upstream change (drives most resolutions)
v0.20.0 is a **decomposition release**: the update pipeline moved out of `hermes_cli/main.py`
(16.7k → 12.6k lines) into new `hermes_cli/update_cmd.py` / `update_lock.py` / `npm_engine.py` /
`_startup_fast.py`; web routes were extracted to `hermes_cli/web_routers/`; the test suite was
pruned massively (46.8k → 19.8k test functions). Consequently many OTTO customizations had to be
**ported to new module locations** (done, verified by greps + tests):
- otto update-branch defaults → `update_cmd.py` `_cmd_update_check` (+ main.py's kept copy)
- cmetech ZIP URL + otto/main branch allowance → `update_cmd.py` `_update_via_zip`
- cmetech `OFFICIAL_REPO_URLS` entries + fork-detection comment → `update_cmd.py`
- `_uv_subprocess_env` (python-isolation) → defined in `update_cmd.py`, imported by main.py;
  applied at both moved uv call sites
- brand-aware version label → `_startup_fast.print_fast_version_info`
- skills displayName rename → `web_routers/skills.py`; provider-reload/rollback →
  `web_routers/tools.py` (via upstream's late-binding seam)

## Convergent fixes (upstream fixed what we'd fixed) — decision applied
1. **npm lockfile-dirtying fallback**: upstream now uses `--no-save` (PR #65595) for the exact
   bug our `--no-package-lock` fixed → **took upstream, deleted ours**. CLAUDE.md row + the merge
   skill's `no-package-lock` grep are now stale (follow-up below). Our tolerant
   `test_web_ui_build` assertion already accepted either flag.
2. **install.sh launchers**: upstream added hardcoded `hermes-agent`/`hermes-acp` shims; our
   generic `[project.scripts]`-driven launcher-writer supersedes them → **kept ours, dropped
   upstream's redundant blocks**.
3. **TUI spawn env**: upstream's new `build_subprocess_env(inherit_profile_home=True)` factory +
   our per-brand `HERMES_HOME` setdefault → **layered (upstream structure + our delta)**.
4. **`git pull` → `merge --ff-only origin/<branch>`** in the update flow; our otto-branch tests
   adapted to assert the new command shape (still pinning `origin/otto`).
5. **dashboard_auth/audit.py**: both sides switched to `get_hermes_home()` → took upstream's.
6. **update-branch derivation in autostash tests**: re-applied our `UPDATE_BRANCH =
   _resolve_update_branch(...)` pattern onto upstream's pruned/reorganized test file.

## Ledger decisions (overlap.json: 101 overlaps — 54 none, 45 same_file, 2 owned_symbol; 7 decisions)
- `brand-aware-version-label`: **adapt** (label ported into `_startup_fast`)
- all six others (`workflow-language-*`, `workflow-parser-backed-symbol-ownership`): **preserve**
  (verified present post-merge; parser pins micromark 4.0.2 / typescript 6.0.3 / unified 11.0.5 intact)
Recorded in `.git/workflow-merge-evidence/decisions.json`.

## Pre-existing infrastructure defects SURFACED by the trial (not caused by the merge)
1. **Workflow ledger baseline stale** — `last_verified_upstream = aaf5691`; v0.19.0's merge never
   ran the post-merge `--set-verified-upstream b7a05b6b6`. The real Stage-1 records the new
   candidate and fixes this going forward.
2. **`validate_diff_coverage` (the checker's `--diff` mode) structurally cannot pass on this
   repo** — with `--diff <upstream>..base` it sweeps the ENTIRE fork history (~480 offending
   commits incl. the original branding commits); the exclusion list was only ever maintained for
   workflow-project side commits, and the routine gate (`test_workflow_merge_gate.sh`) never
   passes `--diff`. The skill's Stage-0/Stage-1 blocks as written will always exit 1 here.
   Trial workaround: ran `--upstream-diff` only (which works and produced the overlap report).
   Needs a real design decision (fix checker scoping, or drop `--diff` from the skill's blocks).
3. **python-isolation.yaml checker fails even against pristine `base`** — the checker's
   `_exact_pattern` lookbehind `(?<![A-Za-z0-9_$])` can never match PowerShell's
   `$InheritedPythonEnvVars` (likely regressed by the 07-27 evidence-hardening commits).
   Checker fix needed; NOT a merge issue. (browser-profiles + desktop-loader + workflow
   manifests all pass.)
4. **Stale skill grep**: Stage-1's `git grep "'Co-Worker'" scripts/brand/emitters/package-json.mjs`
   asserts the v1.0.5 design that was deliberately REVERSED on 2026-07-15 (per-brand
   shortcutName). The emitter is correct; the skill grep must be updated.

## New surface introduced by this upstream (take-as-is; decisions noted)
- `optional-skills/` tree: upstream moved many bundled skills out of `skills/` (several overlap
  our `curation.skills.exclude` list — filter is fail-open, still correct; list can be slimmed later).
- New modules: `update_cmd.py`, `update_lock.py`, `npm_engine.py`, `_startup_fast.py`,
  `web_routers/`, `observability/`, `subcommands/`, `model_setup_flows.py`, wake-word extra,
  `vercel_sandbox` environment (given our `clean=` kwarg integration + test shim), root eslint
  toolchain, `--force-commit` installer flag, per-slot MoA `enabled` flags, `webhooks` desktop view.
- `apps/desktop/src/lib/version-status.ts`: now lightly OTTO-owned (one-condition widening for
  the release-mode `(update)` chip) — needs a CLAUDE.md/ledger row.

## Risks / work the real run needs
- The real Stage-1 must skip the checker's `--diff` coverage validation (pre-existing defect #2)
  and still do: `--upstream-diff` overlap report ✓ (done), decisions.json ✓ (done),
  `--set-verified-upstream 36cb5ae55` + evidence commit, `test_workflow_merge_gate.sh --phase base`
  (needs a clean committed tree — could not run mid-merge on the trial).
- **Beyond-conflict work is NOT covered by rerere replay**: the new-module ports, package.json
  duplicate-key fix, integration fixes in non-conflicted files (`hermes_state.py` ambiguous-column
  fix, `tests/agent/test_provider_attempt_transport.py` contract update, `vercel_sandbox.py`,
  `version-status.ts`, `tools/managed_process.py`), and ~10 test adaptations. A discard-and-redo
  merge would have to reproduce ALL of that by hand. The trial tree already contains it, gated.
- Deferred post-merge smoke items (Stage 3 on otto): sidebar resume behavior (session-states
  resolution took upstream's supersede), desktop `tsc --noEmit` (build passed; explicit typecheck
  not separately run), `otto` CLI + desktop LAUNCH with gateway.
- Housekeeping observed in passing (pre-existing, decide separately): `.superpowers/brainstorm/`
  server.pid/port state files and an empty `ui-tui/node_modules` path are committed on base.

## Recommendation:  **GO-WITH-FIXES** — specifically: PROMOTE THIS TRIAL as the real merge
Every Stage-1 gate + the build sweep + ~1,600 tests were run deliberately on this tree (the
skill's stated condition for keeping a trial). Promoting = commit the merge on the trial branch,
run the workflow evidence commit (minus the structurally-broken `--diff` validation), run
`test_workflow_merge_gate.sh --phase base` on the committed tree, fast-forward `base`, then
proceed to Stage 2 (per-brand restamp + gates), Stage 3 (build + LAUNCH), Stage 4 (final report).
A fresh-replay merge is the riskier path here: rerere covers only the 82 conflicted files, not
the extensive beyond-conflict porting this upstream demanded.
