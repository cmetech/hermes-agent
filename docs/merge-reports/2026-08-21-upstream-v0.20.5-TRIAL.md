# TRIAL merge report — upstream v0.20.5 → `base` (2026-08-21)

> Dry run only. No upstream merge landed on `base`, no release was built, and
> nothing was pushed. The disposable checkout was removed after evidence was
> captured.

## Verdict: NO-GO pending a second repaired rehearsal

The Git merge is mechanically resolvable: all 100 conflicted paths were
resolved, the merge commit had no unmerged entries, and the merged tree passed
16 of 17 customization ledgers. It is not yet safe to perform the real merge.
The sealed `workflow-orchestration` ledger found 17 failing invariant files,
including real regressions in Kanban stale-run compare-and-swap behavior,
workflow provider-attempt accounting, read-only API behavior, and bundled
connector registration.

The next action is to repair these findings in another isolated rehearsal and
rerun every gate. Step 9, the real merge into `base`, remains blocked until that
rehearsal is fully green and explicitly approved.

## Immutable scope

- Downstream parent: `c1d29c2b0da0d021cfc0153c7d8751eb84b323da`
- Last merged upstream baseline: v0.20.0,
  `36cb5ae5530a75def7df3195e49b7a4aa2add482`
- Assessed upstream release: v0.20.5,
  `fcbd1076a93841fa88855acce810e342a5b78101`
- Disposable merge commit:
  `54f27cda750d6c7f41a56050985dd8e605ff5a5d`
- Disposable merge tree:
  `44fc5d5345eb65a1ddd25c63b01d0e6249858579`
- Upstream range: 3,673 commits, 3,962 files changed, 519,277 insertions,
  and 96,765 deletions
- Merge result relative to downstream parent: 3,983 changed paths

The v0.20.5 commit was used directly. A newer `origin/main` tip was not
substituted.

## Preflight and overlap review

- All 17 customization manifests passed strict validation on committed `base`.
- Every manifest baseline was verified as an ancestor of the pinned v0.20.5
  target.
- The neutral desktop brand gate passed from the detached preflight checkout.
- Eleven overlap reports contained 176 overlap rows and 41 decision-required
  rows. Every row was reviewed before conflict resolution.
- Preflight result: `PREFLIGHT_OK manifests=17 decision_reports=11`.

The review used the five durable dispositions now documented by the merge
skill: `take-upstream`, `union-adapt`, `remove-downstream-as-equivalent`,
`retain-downstream-behavior`, and `blocked-unclear`. Retaining a fork behavior
means adapting the merged tree while preserving full upstream ancestry; it
does not omit or revert an upstream commit.

## Conflict result

- Initial conflicts: 100 paths, 237 conflict hunks
- Final unmerged entries: 0
- Resolution summary: 97 union/adapt resolutions, two fork-exact resolutions
  (`hermes_cli/__init__.py` and
  `tests/hermes_cli/test_startup_plugin_gating.py`), and one upstream-exact
  resolution (`apps/desktop/src/store/statusbar-prefs.ts`)
- All 100 conflict resolutions were recorded by rerere.

Important union/adapt work completed in the disposable tree included:

- Combined upstream plugin lifecycle, dependency, unload, and ledger work with
  fork profile scoping, fail-closed collision handling, background-service
  ownership, and configuration safety.
- Preserved single-fire plugin tool admission while carrying upstream
  `modified_args`; fixed the sequential middleware executor call shape.
- Combined upstream Kanban descendant invalidation and worker termination with
  the fork's compare-and-swap design. The later invariant failure shows that
  the stale-run block path still needs repair.
- Combined installer fingerprint skipping with fork fail-hard diagnostics,
  timeouts, and subprocess isolation.
- Kept the fork's profile-scoped provider ownership and made bundled provider
  precedence deterministic over pip entry-point impostors.
- Kept `User-Agent: HermesAgent/<version>` for Kimi requests but rejected the
  upstream `HTTP-Referer` and `X-Title` attribution headers. Those headers are
  outbound product attribution and conflict with the fork's opt-in telemetry
  rule.
- Accepted upstream `--version`/`-V` and removed the redundant `hermes version`
  path as upstream-equivalent.
- Preserved the branded package version (`4.2.2`) and neutral branch-generation
  rules.
- Adapted desktop structured API handling, plugin routes, notification error
  behavior, profile-scoped stores, and workflow route contribution reactivity.
- Preserved environment attribution through `builtin export` and bounded
  stdin-error diagnostics.

No upstream ancestry was intentionally omitted. The Kimi header decision is a
behavioral reconciliation at an active fork policy boundary.

## Verification evidence

### Passed

- Core conflict-surface Python suite: 1,458 passed, 9 skipped.
- Additional plugin/API/Kanban suite: 305 passed.
- Credential/plugin/provider focused suite: 180 passed.
- Environment focused suite: 106 passed.
- Plugin setup-action suite: 21 passed.
- Python isolation focused suites: 38 passed in aggregate.
- Kimi attribution policy tests: 7 passed.
- Sealed customization ledgers: 16 manifests fully passed.
- Across all sealed ledgers: 369 records, 364 executable checks, 347 passed,
  17 failed, and 5 reference-only records.
- Merge index: zero unmerged entries.

These passing results establish that most fork contracts survived. They do not
override the failed release-integrity gates below.

### Failed release-integrity gate

`workflow-orchestration` executed 235 checks: 218 passed and 17 failed. A
direct exact-tree reproduction over the 12 Python files ran 1,830 tests and
reported 1,798 passed, 1 skipped, and 31 failed tests. A direct exact-tree run
over the five desktop files reported 105 passed and 46 failed tests.

Failed invariant files:

1. `apps/desktop/src/app/settings/model-settings.test.tsx`
2. `apps/desktop/src/app/workflows/index.test.tsx`
3. `apps/desktop/src/app/workflows/view-workflow-dialog.test.tsx`
4. `apps/desktop/src/components/assistant-ui/embeds/mermaid-embed.test.tsx`
5. `apps/desktop/src/components/ui/zoomable.test.tsx`
6. `tests/agent/test_provider_attempt_transport.py`
7. `tests/hermes_cli/test_banner_git_state.py`
8. `tests/hermes_cli/test_kanban_core_functionality.py`
9. `tests/hermes_cli/test_kanban_stale_run_callers.py`
10. `tests/hermes_cli/test_tui_resume_flow.py`
11. `tests/plugins/workflow/test_ericsson_connector_toolsets.py`
12. `tests/plugins/workflow/test_node_mcp.py`
13. `tests/plugins/workflow/test_phase3_bash_substitution.py`
14. `tests/plugins/workflow/test_retry.py`
15. `tests/plugins/workflow/test_workflow_language_desktop_e2e.py`
16. `tests/scripts/test_workflow_merge_gate.py`
17. `tests/test_hermes_state.py`

The Node install also reported four high-severity dependency audit findings.
Those are not proven merge regressions, but they require a separate dependency
review before a branded release.

## Blocking findings and importance

| Importance | Finding | Evidence | Required disposition |
|---|---|---|---|
| Critical | Workflow provider-attempt accounting is no longer one durable ledger charge per actual workflow provider call. Upstream automatic session-title generation adds calls, and grant-limited runs exhaust early. | All ten parameterizations in `test_real_inline_worker_tree_charges_one_durable_provider_ledger` failed; the MCP test observed three provider requests instead of two. | `union-adapt`: distinguish auxiliary title work from the sealed workflow budget without weakening the budget authority. |
| High | A stale Kanban run can block/heartbeat a newer attempt, and CLI/tool callers return a generic error instead of the winning-state conflict. | Core CAS test did not raise `TaskMutationConflict`; three caller-contract tests failed. | `union-adapt`: restore the run-id compare-and-swap guard in the shared mutation path and retain upstream descendant invalidation. |
| High | Bundled Ericsson connector plugins are rejected because upstream now requires `config_schema` to be a mapping. The GitLab write tool is consequently absent. | Discovery logged all Ericsson schemas as invalid; `gitlab_create_branch` was missing from the registry. | `union-adapt`: migrate the bundled descriptors to the upstream schema contract and rerun admission/capability tests. |
| High | The fork's native workflow portability gate still reads `.github/workflows/ci.yml`, while upstream renamed/restructured CI as `.github/workflows/ci.yaml`. | Six merge-gate tests failed because `ci.yml` was absent. | `union-adapt`: port the portability matrix/job to upstream `ci.yaml` and update the single authoritative test path. |
| High | Managed relay tests do not supply the new upstream operation lease, so terminal provider-capability drift is no longer tested through the real merged contract. | `test_relay_stream_preserves_terminal_route_drift` fails at `acquire_operation_lease`. | Update the test fixture to the upstream lease contract, then verify the original drift exception still wins. |
| Medium | A read-only desktop workflow-language request creates `cache/schema_columns.json` under `HERMES_HOME`. | The before/after home snapshot gained the cache directory and file. | Determine whether the cache is required. If not, make schema discovery read-only; if required, update the invariant only after proving no user config mutation or authority expansion. |
| Medium | Desktop tests contain stale mocks/selectors after upstream introduced cache writers, explicit `scopeProfile`, shared dialog body scrolling, and accessible `aria-label` triggers. | Missing mock export, profile-cache mismatch, shell-vs-body overflow assertion, title-vs-role selector, and whole-module Mermaid mock failures. | Mostly `remove-downstream-as-equivalent` test migration; retain the profile-bound cache and stale-response behavior as real invariants. |
| Medium | Termux fast-start tests still assert the removed command-form version path, omit the new `skills` argument, and expect an old brand label. | Three fast-start tests failed. | Update tests to `--version`/`-V`, the current light-parser payload, and generated brand metadata. |
| Medium | Banner and FTS tests assert old implementation details rather than the retained behavior contract. | Banner mocks reject the new shallow-repository probe; the FTS trace looks for an obsolete SQL prefix. | Update mocks/assertions only after confirming ahead/behind safety and context enrichment behavior remain intact. |
| Medium | The phase-3 bash substitution invariant failed only under sealed per-file execution and passed in the grouped direct run. | Sealed runner failed twice; grouped direct run did not reproduce. | `blocked-unclear`: reproduce under the sealed environment; do not label it flaky or waive it without a root cause. |

## Decision-required catalog coverage

The 41 required rows are represented by the existing manifests and remain
`adapt`/`preserve` candidates for the repaired rehearsal:

- Browser: `enrolled-browser-launch-wiring`
- Credentials: `plugin-secret-authority-and-tiering`,
  `plugin-secret-migration-and-process-isolation`,
  `atomic-private-credential-publication`
- Desktop test infrastructure:
  `native-notification-dispatch-rejection-guard`
- Environments: `authenticated-shell-session-snapshots`,
  `ssh-non-posix-account-shell`
- Fork runtime: `profile-distribution-skill-ownership`,
  `s6-supervise-permission-finalization`, `branded-cli-and-profile-labels`,
  `branded-native-notification-title`,
  `desktop-statusbar-default-and-locked-pills`,
  `desktop-full-page-session-restore`
- Kanban: `kanban-attachment-reveal`, `kanban-attachment-preview`
- Plugin configuration: `static-plugin-configuration-descriptors`,
  `owner-bound-plugin-application-commands`,
  `plugin-cli-command-collision-rejection`,
  `profile-plugin-configuration-and-setup-actions`,
  `desktop-plugin-configuration-projection`,
  `optional-plugin-capability-staging-and-vendoring`
- Python isolation: `desktop-backend-python-env-isolation`,
  `import-safe-runtime-modules`
- Test harness: `parallel-test-runner-resource-policy`
- Tool choice: `one-shot-tool-choice-surfaces`,
  `provider-tool-policy-transport`,
  `api-tool-contract-lifecycle-and-diagnostics`,
  `native-structured-output-transports`
- Workflow orchestration: `workflow-phase5-sealed-runtime-context`,
  `brand-aware-version-label`, `desktop-workflow-test-gate`,
  `workflow-language-admission-pinning`, `workflow-language-schema-cli`,
  `workflow-language-desktop-status`,
  `workflow-language-desktop-capability-skew`,
  `workflow-phase4-language-contracts`,
  `workflow-language-regression-gates`,
  `workflow-parser-backed-symbol-ownership`,
  `plugin-tool-admission-decisions`,
  `desktop-workflow-catalog-pagination-and-filter`,
  `desktop-workflow-diagram-viewport-fit`

## Catalog changes required before the real merge

The per-release report owns this historical decision record. The repaired
merge should update manifest guidance only for ongoing ownership:

1. Add the Kimi honest-user-agent/no-attribution-header rule to the appropriate
   provider/tool-policy invariant. Revisit only after a generic user-facing
   telemetry opt-in exists.
2. Record deterministic bundled-provider precedence and read-only entry-point
   discovery as an ongoing plugin/provider invariant.
3. Record stale-run compare-and-swap at the shared Kanban mutation boundary;
   caller-specific tests are secondary.
4. Record provider-budget treatment for upstream-created auxiliary calls such
   as session-title generation.
5. Update the native workflow CI path and portability-job ownership from
   `ci.yml` to the upstream `ci.yaml` structure.
6. Keep environment `builtin export` attribution and bounded stdin diagnostics
   in `environment-runtime`; they remain active fork behavior.
7. Keep desktop profile-bound cache keys and reactive contributed routes as
   behavior contracts, while removing selectors/mocks that only detect old
   implementation details.

## Required next rehearsal

1. Start from the same downstream parent and pinned v0.20.5 commit.
2. Replay the recorded conflict resolutions, then implement the blocking fixes
   above in the isolated tree.
3. Run all 17 sealed manifests until all executable checks pass; no waived
   sealed failure is acceptable.
4. Run the neutral brand gate, Python parse/import checks, shell/PowerShell
   parser checks, desktop typecheck/test/build/lint, and
   `scripts/test_workflow_merge_gate.sh --phase base` on the committed tree.
5. Review the four high-severity npm audit findings separately.
6. Produce an updated `GO` or `GO-WITH-FIXES` trial report and request explicit
   approval before step 9.

## Checkout and cleanup

The disposable worktree and its external Node dependency tree were removed.
The live checkout remained on `base`; pre-existing untracked user files were
left untouched.
