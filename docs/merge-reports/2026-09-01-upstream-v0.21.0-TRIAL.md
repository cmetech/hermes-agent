# TRIAL merge report — upstream v0.21.0 → `base`

> Rehearsal completed 2026-09-01. Dry run only: the upstream merge did not
> land on live `base`, no brand ref moved, no release was built for
> publication, and nothing was pushed.

## Verdict: GO for the real `main` → `base` merge after explicit approval

The exact upstream v0.21.0 release can be merged into `base`. The disposable
candidate has the required downstream and upstream parents, a clean neutral
tree, all 17 customization ledgers sealed, the canonical base gate passing,
and both `otto` and `loop24` overlays regenerating and passing their brand
gates.

This verdict authorizes only the real ancestry-preserving merge after human
approval. It does not authorize a push, brand-ref update, package
publication, or release.

## Immutable scope

- Rehearsal downstream parent: `89f2cb6ea9ebb65681cee8b60305ca33e56870c5`
- Last verified upstream baseline: v0.20.6,
  `5fc308a70719a83cccdbba4c0e39c23f5a8239d5`
- Assessed upstream release: v0.21.0,
  `29112bef099274229cadff79cdff7bf7b99c4b77`
- Annotated release-tag object:
  `6e8f8418e6378eb2617e4de074e13dedd091b8af`
- Release notes:
  <https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.31>
- Disposable merge commit:
  `f9280e3d07cd95f6c7197c43c6cfb30abc70980f`
- Disposable merge tree:
  `2c1cd6fe736f329cb759608eabeb14b91d1adca5`
- Candidate parents: downstream `89f2cb6ea9…`, upstream `29112bef09…`
- Upstream range: 911 commits, including 541 `fix`, 86 `feat`, 50
  `refactor`, 91 `test`, 36 `docs`, and 54 `chore` subjects
- Upstream range footprint: 1,895 files changed, 193,147 insertions, and
  48,321 deletions
- Candidate relative to downstream parent: 1,902 files changed, 193,193
  insertions, and 48,361 deletions

The target was pinned directly. `origin/main` is currently
`b20cc5f787ea816ea8645603b7b2ac8234dcb8b4`, 55 commits beyond v0.21.0,
and was not substituted into the rehearsal.

## Release assessment

The v0.21.0 notes describe the Pantheon release: Bot Mode and agent group
chats, `hermes peer`, cron continuity and memory, live subagent steering, a
unified MCP command center, an agent-driven Desktop browser, new providers and
models, CLI/TUI improvements, and broad security/reliability hardening. They
also state that the release rolls up the v0.20.1-v0.20.6 patch infrastructure.

Because this fork already merged v0.20.5 and v0.20.6, this rehearsal did not
reconstruct the release from prose or moving `main`. It merged only the exact
Git interval `v0.20.6..v0.21.0`, preserving release ancestry while reviewing
the remaining 911 commits against the fork's active contracts.

## Preflight, overlap, and conflict review

- All 17 customization manifests passed strict validation on committed
  `base`; every recorded baseline resolves to the verified v0.20.6 commit and
  is an ancestor of v0.21.0.
- The detached neutral Desktop brand gate passed.
- Thirty overlap rows required explicit review: one browser-profile row, two
  credential rows, one environment row, seven fork-runtime rows, two Kanban
  rows, five plugin-configuration rows, one Python-isolation row, four
  tool-choice rows, and seven workflow-orchestration rows.
- Every decision-required row was dispositioned as `adapt`: preserve the
  fork's active contract while incorporating the upstream behavior. No row
  was classified as upstream-equivalent.
- The textual merge produced 30 content-conflicted paths. Every conflict was
  reconciled manually; no shared source or ledger-owned file used whole-file
  `ours`/`theirs` resolution.
- Final candidate state: zero unmerged entries; both parents are ancestors;
  the tree is neutral and clean; `git diff --check` passes.

The 30 explicitly reviewed overlap contracts were:

| Manifest | Reviewed contracts | Disposition |
|---|---|---|
| Browser profiles | `enrolled-browser-launch-wiring` | Adapt |
| Credential storage | `plugin-secret-migration-and-process-isolation`; `atomic-private-credential-publication` | Adapt |
| Environment runtime | `authenticated-shell-session-snapshots` | Adapt |
| Fork runtime | profile skill ownership; shutdown watchdog reaping; s6 permission finalization; branded CLI/profile labels; branded notifications; full-page session restore; Skills Hub suppression | Adapt |
| Kanban attachments | reveal and preview contracts | Adapt |
| Plugin configuration | static descriptors; owner-bound commands; collision rejection; profile setup/actions; Desktop projection | Adapt |
| Python isolation | `import-safe-runtime-modules` | Adapt |
| Tool choice | one-shot surfaces; provider transport; API lifecycle/diagnostics; native structured output | Adapt |
| Workflow orchestration | Desktop test gate; admission pinning; schema CLI; Desktop status; capability skew; phase-4 language contracts; plugin admission decisions | Adapt |

Conflict concentration:

| Area | Paths | Main contracts reviewed |
|---|---:|---|
| Desktop | 7 | settings, intro/i18n, slash commands, Kanban API, test configuration, package identity |
| Agent/gateway/TUI | 8 | API run contracts, provider headers, shutdown, one-shot tool choice, prompt and server routing |
| CLI/provider runtime | 7 | authentication aliases, commands, model switching/catalogs, plugin projection, web server |
| Install/environment/lock | 5 | POSIX and Windows installers, authenticated shell snapshots, version ownership, lockfile |
| Regression tests | 3 | Anthropic adapter, profile overrides, update quarantine/provider profiles |

## Important reconciliations

| Importance | Surface | Rehearsed disposition |
|---|---|---|
| Critical | API tool contracts | Upstream split `/v1/runs` into `gateway/platforms/api_server_runs.py`; the fork's strict contract parser, idempotency fingerprint, policy validation, and operation context were ported to the new shared module instead of being left behind in the old route file. |
| Critical | Environment isolation | Combined upstream's bounded synchronous hard stop with authenticated snapshot sentinels, clean-shell construction, interrupt propagation, and secret-safe Docker environment handling. |
| High | Tool-choice routing | Preserved the fork's one-shot CLI, Desktop, gateway, and TUI contract while taking upstream command and transport changes. |
| High | Provider/runtime transport | Kept fork routing and Codex header policy while incorporating upstream provider aliases, model catalog updates, Anthropic credential-module split, and response handling. |
| High | Plugin and workflow surfaces | Preserved owner-bound plugin configuration, collision checks, workflow admission, schema/status projection, and Kanban attachment behavior across upstream module changes. |
| High | Desktop settings | Unified the browser settings surface, including upstream Brave Origin support, without duplicating the `browser` section or losing the fork's enrolled-profile safety fields. |
| Medium | Install/update | Preserved the fork's point-of-use Node selector while adopting upstream's stronger centralized Node-plus-npm readiness validation. |
| Medium | Skills | Updated workflow fixtures for upstream's move of `ascii-art` from bundled to optional skills while retaining distribution ownership checks. |
| Medium | Version ownership | Retained fork version `4.2.2` across Python, Desktop, CLI, and `uv.lock`; upstream v0.21.0 is represented by Git ancestry. |

## Issues found during rehearsal

| Importance | Finding | Result |
|---|---|---|
| High | The old API run-route conflict would have silently dropped the fork's strict tool contract because upstream moved the implementation to a new module. | Ported the complete contract to `api_server_runs.py`; tool-choice ledger and canonical merge gate pass. |
| High | The combined environment implementation initially exposed upstream's new `cwd` result and clean-shell literals to exact-dictionary/secret assertions. | Adapted assertions to the stronger behavior while keeping secret values excluded; 137 environment tests pass. |
| High | Workflow tests still copied `skills/creative/ascii-art`, but upstream moved it to `optional-skills`. | Updated the fixtures to the authoritative location; workflow ledger and canonical gate pass. |
| Medium | Anthropic tests patched the former refresh symbol after upstream split credential refresh into `agent.anthropic_credentials`. | Patched the new authority; the six affected workflow/credential/packaging files pass 131 tests. |
| Medium | The Windows installer test expected point-of-use npm selection only and rejected upstream's centralized readiness helper. | Kept both protections and asserted the stronger combined behavior; all 16 Windows npm guard tests pass. |
| Medium | The merged Desktop settings table contained two `browser` keys. | Removed the duplicate and retained one unioned section; typecheck and the full Desktop suite pass. |
| Medium | The exact `keyring` pin needed an exclusion exception for the repository's date-bounded lock policy. | Added `keyring = false` under `tool.uv.exclude-newer-package`; `uv lock --check` passes. |
| Infrastructure | A concurrent macOS ledger run hit a native SQLite bus error. | Re-ran the affected ledger serially against the exact candidate; it passed. No product failure reproduced. |
| Infrastructure | Desktop dependencies had been installed with `--ignore-scripts`, leaving Electron's package present without its binary. | Rebuilt Electron and reran the two affected suites (22/22), then reran the full Desktop suite successfully. |

## Exact-candidate verification

All results below were obtained against disposable commit
`f9280e3d07cd95f6c7197c43c6cfb30abc70980f` after its final code change.

### Customization and canonical merge gates

- 17 merged-state ledger reports.
- 370 total records: 365 executable and five reference-only.
- 365/365 executable checks passed; zero failures.
- Canonical base gate: 4,362 tests passed, zero failures, eight explicit
  Linux/Windows platform skips across 83 files.
- Installed-distribution gate: three passed, zero failures.
- Merge-gate Desktop matrix: 211 passed across 11 files.
- Canonical evidence sealed
  `TESTED_BASE_SHA=f9280e3d07cd95f6c7197c43c6cfb30abc70980f`.

### Desktop, targeted, and dependency checks

- Full Desktop Vitest suite: 860 files passed, two platform files skipped;
  9,261 tests passed and six platform tests skipped.
- TypeScript renderer/Electron/E2E typechecks passed.
- Production renderer, Electron main/preload, and native dependency staging
  passed; `assert-dist-built` passed.
- Focused Electron regression rerun: 22/22 passed.
- Base/Docker environment suite: 137/137 passed.
- Windows npm guard suite: 16/16 passed.
- Affected workflow, credential, and packaging group: 131/131 passed.
- `uv lock --check`, POSIX installer syntax, merge whitespace checks, and AST
  parsing for all 825 changed Python files passed.

### Branding

- Neutral `base` brand gate passed with no stamp on the candidate tree.
- OTTO generator and brand gate passed at the exact candidate SHA; 141
  provider/runtime/skin tests passed and the messaging catalog contains the
  required 11 platforms.
- LOOP24 generator and brand gate passed at the exact candidate SHA; 125
  provider/runtime tests passed and the messaging catalog contains the
  required 11 platforms.
- Both generated overlays contain the exact tested base and match it on every
  generic workflow/runtime path. All eight generator emitters passed for each
  brand.

## Remaining risk and real-merge controls

- Darwin cannot execute the explicitly Linux-only and Windows-only checks;
  existing Linux and Windows CI lanes remain required before release.
- This is still a large interval (911 commits), even though the prior patch
  merges absorbed much of its functional surface. The 17 ledgers and the
  ancestry-pinned merge reduce that risk but do not replace post-merge CI.
- The exact upstream target contains two accidentally named mutex fixture
  files under `apps/desktop/'/var/folders/...`. They were preserved because
  they are part of the pinned release tree; test-created copies were removed
  from the disposable worktrees. They can be cleaned in a separate,
  intentional follow-up rather than silently changing the upstream merge.
- Vite reports an existing future `configLoader: native` compatibility
  warning and `advancedChunks` deprecation during build. Neither failed the
  current production build.
- The real merge must reproduce candidate tree
  `2c1cd6fe736f329cb759608eabeb14b91d1adca5` before manifest baselines or
  brand refs advance.
- The customization manifests intentionally remain at the v0.20.6 baseline
  during rehearsal. Advance them only after the real merge and all
  real-commit gates pass.

## Real-merge replay requirements

After explicit approval:

1. Confirm live `base` still contains rehearsal parent `89f2cb6ea9…` plus
   this report and has no intervening code changes.
2. Merge exact target `29112bef099274229cadff79cdff7bf7b99c4b77`;
   do not use moving `origin/main`.
3. Replay the reviewed conflict resolutions and adaptations, then verify both
   parents, zero unmerged entries, and candidate-tree equivalence before
   committing.
4. Re-run the merge-critical seals on the real commit.
5. Advance all 17 manifest baselines only after the real merge passes.
6. Stop again before brand-ref updates, packaging for publication, pushing,
   or release unless those actions are separately approved.

## Checkout and cleanup

The disposable merge, preflight, evidence, and brand worktrees and their
generated artifacts are removed after this report is saved. The live checkout
remains on `base`; the user's pre-existing untracked paths are preserved.
