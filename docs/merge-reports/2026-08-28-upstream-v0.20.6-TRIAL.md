# TRIAL merge report — upstream v0.20.6 → `base`

> Rehearsal completed 2026-08-28. Dry run only: the upstream merge did not
> land on live `base`, no brand ref moved, no release was built for
> publication, and nothing was pushed.

## Verdict: GO for the real `main` → `base` merge after explicit approval

The exact v0.20.6 release can be merged into `base`. The disposable candidate
has the required downstream and upstream parents, a clean neutral tree, all 17
customization ledgers sealed, the canonical base gate passing, and both `otto`
and `loop24` overlays regenerating and passing their brand gates.

This verdict authorizes only the real ancestry-preserving merge after human
approval. It does not authorize a push, brand-ref update, package publication,
or release.

## Immutable scope

- Rehearsal downstream parent: `49444cbf7e5c5f0621f33283a9fdb11828d6c3fc`
- Last verified upstream baseline: v0.20.5,
  `fcbd1076a93841fa88855acce810e342a5b78101`
- Assessed upstream release: v0.20.6,
  `5fc308a70719a83cccdbba4c0e39c23f5a8239d5`
- Disposable merge commit:
  `c4dce82d27f1b49049a532a99c791f2db3c29a93`
- Disposable merge tree:
  `a75c3e3a945d350ee65c9d201dad50ee20dc9ef7`
- Candidate parents: downstream `49444cbf7e…`, upstream `5fc308a707…`
- Upstream range: 1,376 commits, including 824 `fix`, 116 `feat`, 47
  `refactor`, 124 `test`, 26 `docs`, and 90 `chore` subjects
- Upstream range footprint: 1,558 files changed, 176,755 insertions, and
  21,324 deletions
- Candidate relative to downstream parent: 1,560 files changed, 176,730
  insertions, and 21,313 deletions

The target was pinned directly. `origin/main` is currently
`35328345d5e3b5badc47271bdb8828e1fd2d25f4`, 223 commits beyond v0.20.6,
and was not substituted into the rehearsal.

## Preflight, overlap, and conflict review

- All 17 customization manifests passed strict validation on committed
  `base`; every baseline is the v0.20.5 commit and is an ancestor of v0.20.6.
- The detached neutral Desktop brand gate passed.
- The 17 overlap reports contain 176 rows: 76 no-overlap, 90 same-file, and
  ten owned-symbol rows.
- Thirty-five rows required explicit review: 25 same-file and ten
  owned-symbol overlaps.
- The textual merge produced 37 content-conflicted paths. Every conflict was
  reconciled manually; no shared source or ledger-owned file used whole-file
  `ours`/`theirs` resolution.
- Final candidate state: zero unmerged entries; both parents are ancestors;
  `git diff --check` is clean.

Conflict concentration:

| Area | Paths | Main contracts reviewed |
|---|---:|---|
| Agent runtime | 4 | Codex request transformation, provider-attempt context, error classification, tool admission |
| Desktop | 10 | main-process wiring, plugin APIs, settings/profile routing, status bar, SDK and session fixtures |
| CLI/provider runtime | 10 | parser, commands, config, browser profiles, plugin lifecycle, provider resolution, update/uninstall |
| Root/release/test runner | 4 | version/lock ownership, agent headers, PowerShell install, isolated test execution |
| Regression tests | 4 | classifier, Gemini/Codex transports, Microsoft Graph |
| Tools and lock | 5 | browser, shell environment, Graph client, essential-skill sync, Python lock |

The 35 decision-required rows were dispositioned as `union-adapt`: preserve
the fork's active contract while incorporating the upstream behavior. No
upstream fix was declined because resolution was difficult, and no downstream
contract was removed as equivalent without evidence.

## Important reconciliations

| Importance | Surface | Rehearsed disposition |
|---|---|---|
| Critical | Tool execution and provider contracts | Combined upstream normalized tool-call handling and response metadata with the fork's request-scoped admission middleware, sealed route constraints, contract-echo checks, and provider-attempt accounting. |
| Critical | Prompt/cache and attribution policy | Preserved stable conversation contracts and the fork's no-attribution behavior. Cloudflare headers follow the candidate target URL, while Kimi does not receive upstream attribution headers without an opt-in. |
| High | CLI/provider resolution | Added upstream Bedrock region resolution only on the Bedrock path while retaining credential-free route classification and sealed workflow route authority. |
| High | Browser profiles | Combined upstream real-profile resolution with the fork's enrolled-port ownership and corporate-profile protections. |
| High | Desktop workflow/plugin surfaces | Preserved profile routing, workflow contribution APIs, readiness state, and structured API status while taking upstream automation-stop and timeout behavior. |
| High | Shell environment | Retained the fork's inherited-environment hardening and upstream pager/cwd defaults. The merged shell uses `builtin export`, preventing a shadowable function or executable from intercepting environment setup. |
| High | Install/update | Combined dynamic brand shims with upstream external Hermes launchers, PATH migration, shim quarantine, and stronger isolated `uv` update behavior. |
| Medium | Skills | Took upstream essential-only skill synchronization while retaining fork ownership and distribution invariants. |
| Medium | Version ownership | Retained fork version `4.2.2` across `pyproject.toml`, Desktop metadata, CLI version, and `uv.lock`; upstream v0.20.6 remains represented by Git ancestry. |

## Issues found during rehearsal

| Importance | Finding | Result |
|---|---|---|
| High | Upstream's pager defaults used plain `export` inside the fork's hardened shell initialization. | Changed the shared boundary to `builtin export`; all 74 base-environment tests pass. |
| High | Two downstream workflow MCP fixtures still imported the MCP 1.x `FastMCP` surface after the merged lock moved to MCP 2.0. | Migrated the fixtures to the MCP 2.0 `MCPServer` contract; the 170-test workflow MCP file and the full workflow gate pass. |
| Medium | The upstream Desktop theme test froze `DEFAULT_SKIN_NAME` to `nous`, conflicting with generated brands. | Replaced the brand snapshot with the behavior contract: `nous-alt` is registered and cannot be the active branded default. |
| Medium | The Desktop local-pack test froze `cmetech/otto` repository metadata even though neutral `base` must use the upstream repository and brand generation owns the release repository. | Made the assertion compare Electron Builder's resolved owner/repo to the current package metadata. Neutral and generated brands now share one test. |
| Low | A POSIX managed-update fixture used `/bin/true`, which does not exist on macOS. | Switched the test fixture to portable `/usr/bin/true`; production launcher code was unchanged. |
| Infrastructure | Running Desktop tests concurrently with ledger sealing mutated Vite caches in the external dependency view. | Reinstalled dependencies and ran ledgers serially, matching the merge procedure. No product failure. |
| Infrastructure | The fresh trial venv initially omitted manifest-declared Daytona, ACP, and Bedrock extras, causing lazy-install and collection failures. | Provisioned the pinned extras in the disposable venv. Reproductions then passed without runtime changes. |
| Infrastructure | The package harness correctly rejected a detached build stamp with `branch: null`. | Re-ran the disposable package gate with supported `GITHUB_SHA` and `GITHUB_REF_NAME=base` inputs; the DMG/package assertions passed. |

## Exact-candidate verification

All results below were obtained after the last candidate change and against
disposable commit `c4dce82d27f1b49049a532a99c791f2db3c29a93`.

### Customization and canonical merge gates

- 17 merged-state ledger reports.
- 369 total records: 364 executable and five reference-only.
- 364/364 executable checks passed; zero failures and zero flaky-first
  attempts.
- Canonical base gate: 3,971 Python tests passed, zero failures, eight
  platform skips across 75 files.
- Installed-distribution gate: three passed, zero failures.
- Merge-gate Desktop matrix: 207 passed across 11 files.
- Canonical evidence sealed
  `TESTED_BASE_SHA=c4dce82d27f1b49049a532a99c791f2db3c29a93` and passed its JSON schema.

### Desktop, package, and dependency checks

- Renderer UI: 652 files and 6,520 tests passed.
- Electron platform suite: 133 files passed, two platform files skipped;
  1,964 tests passed and six platform tests skipped.
- Electron `node:test` group: 49 passed.
- Bundled Desktop plugin tests: 625 passed.
- TypeScript renderer/Electron/E2E typechecks passed.
- ESLint passed with zero errors and 205 existing warnings.
- Production renderer, Electron main/preload, and native dependency staging
  passed; `assert-dist-built` passed.
- Branded macOS DMG package verification passed with a branch-bearing build
  stamp.
- Clean `npm ci` installed 1,369 packages and `npm audit` reported zero
  vulnerabilities.

### Branding

- Neutral `base` gate passed: no brand stamp on the candidate tree.
- OTTO overlay gate passed at disposable commit
  `0f16b7f0712156013bb96c9d48234519c623eadb`.
- LOOP24 overlay gate passed at disposable commit
  `a22db5b8b6e08e75865954cd6bcdf3de48a2984b`.
- Both generated overlays contain the exact tested base and match it on every
  generic workflow/runtime path.
- Both package locks regenerated offline and all eight generator emitters
  passed for each brand.

### Additional integrity checks

- Targeted reconciliation suites: 586 Python tests passed.
- Base environment: 74/74 passed.
- Workflow MCP regression file: 170/170 passed.
- Baked workflow seed checks: 16/16 passed.
- Python source compilation, PowerShell parsing, `uv lock --check`, and merge
  whitespace/index checks passed.

## Remaining risk and real-merge controls

- Darwin cannot execute Linux-only and Windows-only tests; eight workflow
  tests and six Electron tests were skipped under their explicit platform
  guards. Existing Linux and Windows CI lanes remain required before release.
- The 205 ESLint warnings are existing non-blocking debt, not new errors.
- The real merge must reproduce candidate tree
  `a75c3e3a945d350ee65c9d201dad50ee20dc9ef7` before manifest baselines or brand
  refs advance.
- The customization manifests intentionally remain at the v0.20.5 baseline
  during rehearsal. Advance them only after the real merge and all real-commit
  gates pass.

## Real-merge replay requirements

After explicit approval:

1. Confirm live `base` still contains rehearsal parent `49444cbf7e…` plus this
   report and no intervening code changes.
2. Merge exact target `5fc308a70719a83cccdbba4c0e39c23f5a8239d5`;
   do not use moving `origin/main`.
3. Replay the reviewed conflict resolutions and adaptations, then verify both
   parents, zero unmerged entries, and candidate-tree equivalence before
   committing.
4. Re-run the merge-critical seals on the real commit.
5. Advance all 17 manifest baselines only after the real merge passes.
6. Stop again before brand-ref updates, packaging for publication, pushing, or
   release unless those actions are separately approved.

## Checkout and cleanup

The disposable merge, dependency, canonical-base, and brand worktrees and
their generated build artifacts are removed after this report is saved. The
live checkout remains on `base`; the user's pre-existing untracked paths are
preserved.
