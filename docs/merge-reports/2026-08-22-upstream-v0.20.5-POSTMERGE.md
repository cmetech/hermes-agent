# POSTMERGE report — upstream v0.20.5 → `base` → branded v7.0.0

## Outcome

The upstream v0.20.5 merge, branded branch regeneration, package/launch gates,
and paired stable v7.0.0 releases completed successfully. The live checkout
returned to `base`. No v7.0.0 product tag or release was created in the source
repository.

## Immutable lineage

- Upstream target: `fcbd1076a93841fa88855acce810e342a5b78101`
  (`0.20.5`).
- Real ancestry-preserving merge: `d247a936c72205c5e94ccfd4d346dbb0df8f16e4`.
- Tested release-time `base`: `ac2d7df293490c95bc7890cdedd92fd7e1a54a01`.
- OTTO v7.0.0 source: `175bec5c465cb621dc95cc2acd484f3ad4d7baeb`.
- LOOP24 v7.0.0 source: `70d6f67d797ae58b85acc1f01f5ead51b10251af`.

The conflict, silent-overlap, importance, applicability, and disposition review
is preserved in `2026-08-21-upstream-v0.20.5-TRIAL.md`. The real merge replayed
that reviewed candidate without substituting moving `origin/main`.

## Post-rehearsal findings

| Importance | Finding | Durable result |
|---|---|---|
| High | Upstream added Desktop `repository.url`; the generator did not own the new field, so LOOP24 initially inherited the OTTO release repository. | `ac2d7df29` added descriptor-driven and neutral repository emission plus LOOP24/neutral assertions. Future generator checks cover the field. |
| High | Repository metadata made Electron Builder's implicit CI publication path reachable. The release workflow already publishes with its own explicit upload step and had no `GH_TOKEN` for Electron Builder. | Release-repo commits `c1d0f336711b3832e030b294f60f48743dd046b2` (OTTO) and `5cfdad5c926feb2f628f9ebba0ccdfa5f433abc6` (LOOP24) pass `--publish never`; the source release guide now requires that preflight. |
| Medium | Three upstream Desktop fixtures asserted neutral Hermes identity after brand generation. | `a3f6759c0`, `cd10cc27a`, `a2408b26c`, and `28a90068e` made the existing release assertions brand-aware without changing runtime behavior. |
| Medium | The cold detached Vitest dependency view was incomplete. | `cfda0deb8` provisions the required cache views; the sealed invariant and complete Desktop suites passed afterward. |
| Release blocker | The rehearsed dependency graph contained patched Electron and Nano ID advisories. | `78d9517b1` upgraded Electron and `cdc52db6b` raised the Nano ID override before branded packaging. |

## Verification

- All 17 strict customization catalogs passed.
- The rehearsal sealed 364/364 executable neutral-base invariant records, with
  five reference-only records.
- Each branded candidate produced 239 ledger records: 233 executable passes,
  four reference-only records, and two neutral-default assertions that are not
  applicable to generated brand branches:
  `tests/hermes_cli/test_apply_profile_override.py` and
  `tests/hermes_cli/test_kanban_db.py`. Both passed on neutral `base`; brand
  identity was covered by the generator and branded Desktop gates. Other
  branded ledger failures remain blockers.
- OTTO and LOOP24 each passed all eight brand-generator emitters.
- Each complete branded Desktop suite passed 7,408 tests with three skips
  across 710 files (709 passed, one skipped).
- Both brands passed package and fresh-launch verification.

## Release evidence

| Brand | Workflow | Release | Assets |
|---|---|---|---:|
| OTTO | [successful run 32583563867](https://github.com/cmetech/otto/actions/runs/32583563867) | [v7.0.0](https://github.com/cmetech/otto/releases/tag/v7.0.0) | 7 |
| LOOP24 | [successful run 32583564671](https://github.com/cmetech/loop24/actions/runs/32583564671) | [v7.0.0](https://github.com/cmetech/loop24/releases/tag/v7.0.0) | 7 |

Initial runs `32583306881` (OTTO) and `32583307967` (LOOP24) were canceled
after the implicit Electron Builder publication failure was identified. The
successful reruns used the release-repository workflow commits named above.
Both releases are stable, name the exact branded source commit in their body,
and contain the expected seven Windows/macOS assets.

## Remaining risks

- The v7.0.0 installers are unsigned, so platform trust warnings remain an
  expected distribution limitation rather than a merge regression.
- Release workflows live outside the source repository. Future releases must
  repeat the documented `--publish never` preflight instead of assuming the
  external workflow still has the fix.
- Neutral-only invariant classification remains deliberately narrow: anything
  without a passing neutral test and an explicit branded replacement gate is a
  blocker.

## Catalog closure and next merge

All 176 entries across the 17 customization manifests now use
`fcbd1076a93841fa88855acce810e342a5b78101` as `last_verified_upstream`.
The next upstream assessment must compare each manifest from that baseline to a
new immutable requested release. It must not re-review v0.20.0 through v0.20.5
or treat v0.20.5 as the next target merely because it appears in historical
reports.
