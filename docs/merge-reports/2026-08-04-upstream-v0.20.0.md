# Upstream merge report — v0.20.0 (2026.8.3)   (merged 2026-08-04)

## 1. Scope
- Upstream range merged: `b7a05b6b6..36cb5ae55` (3301 commits, 4795 files changed).
- Result SHAs: base `d0078169c` · otto `a17fb87bd` · loop24 `5df63e5a1`.
- Tested base SHA (workflow gate certified): `d0078169c` (3,484 tests, 0 failed).
  Tested brand SHAs: loop24 `5df63e5a1` · otto `a17fb87bd`.
- Executed as a promoted trial per the otto-upstream-merge skill's Stage 0.5 (user GO on the
  trial report: `docs/merge-reports/2026-08-04-upstream-v0.20.0-TRIAL.md` — the full
  per-file resolution record lives THERE; this report is the outcome record).

## 2. Conflicts resolved
- `main → base`: **83 files** — the trial report §"Per-file" and its convergent-fix section
  are the authoritative record. Headline: upstream decomposed `hermes_cli/main.py` (update
  pipeline → `update_cmd.py`/`update_lock.py`/`npm_engine.py`/`_startup_fast.py`) and
  `web_server.py` (→ `web_routers/`); every OTTO customization in moved code was ported to
  its new home and verified by grep + test. One genuine silent revert caught: root
  `package.json` duplicate `devDependencies` key (upstream's eslint block) — hand-unioned.
- `base → loop24`: 1 conflict (`apps/desktop/package.json` plist identity) — unioned
  (LOOP24 values + upstream's new `NSCameraUsageDescription`), re-stamped.
- `base → otto`: 2 conflicts (same plist identity + `package-lock.json`) — unioned /
  lockfile regenerated with `npm install` AFTER `generate otto --write` (self-link `otto` ✓).

## 3. Ledger decisions (recorded in .git/workflow-merge-evidence/decisions.json)
- `brand-aware-version-label`: **adapt** — upstream moved `_print_fast_version_info` into
  `_startup_fast`; the brand label (`version_agent_label`) was ported there. Verified live:
  `otto --version` → "Co-worker Agent v4.2.2".
- `workflow-language-admission-pinning`, `workflow-language-schema-cli`,
  `workflow-language-desktop-status`, `workflow-language-desktop-capability-skew`,
  `workflow-language-regression-gates`, `workflow-parser-backed-symbol-ownership`:
  **preserve** — all owned contracts verified present post-merge.

## 4. Gate results
- Stage-1 gate: markers ✓ · py-parse ✓ · channel tests (17) ✓ · release-owner greps 4/4 ✓ ·
  `X-Hermes-Session-Token`=2 ✓ · browser seam greps + `browser-profiles.yaml` checker exit 0 ✓
  → **PASS**
- Workflow base gate: `test_workflow_merge_gate.sh --phase base` → **PASS** (3,484 tests;
  TESTED_BASE_SHA=d0078169c). Two rounds of real regressions were found and fixed first:
  (a) upstream's new Tool Search progressive disclosure deferred request-scoped MCP tools in
  plugin-agent workers, breaking the node contract → pinned off per worker process
  (`agent/plugin_agent_worker.py`, same private-loader seam as the request MCP config);
  (b) merge-union duplicate `getApiRequestProfile` in `apps/desktop/src/hermes.ts` +
  two OTTO workflow UI files needing adaptation to upstream's `react-router`/headerless
  code-card refactors. Desktop `tsc --noEmit` now exits 0.
- Stage-2 per brand: `generate <brand> --check` **8/8 OK** both brands · provider/runtime
  tests ✓ (otto additionally `test_skin_engine.py`) · catalog=11 both brands · brand.json ✓ ·
  curation 13/2/rename ✓ · uninstall identity ✓ · workflow brand gates certified both →
  **PASS**
- Silent-revert grep block: **PASS** (all values correct on both brands; loop24 binary art
  differs from base as required; `.otto`/`.loop24` home stamps correct).
- Stage-3 build: 0 display "Hermes" · 0 ALL-CAPS leaks · release-notes URL ✓ (now in the
  code-split `settings-*.js` chunk) · `/[\\/]OTTO$/i` uninstall guard ✓ · `OTTO.app` 2 /
  `Hermes.app` 0 ✓ · gateway de-brand 0 ✓ · DiscardLocal ✓ → **PASS**
- Stage-3 LAUNCH: gateway started (after its own documented `gw upgrade-env` re-init for the
  new privacy secrets) · `otto -z` streamed the exact requested reply through the gateway ✓ ·
  `otto --version` brand label ✓ · `otto desktop --source` launched, backend booted (plugin
  routes incl. kanban mounted, cron scheduler up), **zero 401s** in gui/agent logs ✓ · only
  warnings: Windows-only `outlook` MCP failing gracefully on macOS (pre-existing, by design)
  → **PASS**

## 5. New surface introduced by this upstream (taken as-is; watch-list)
- New modules: `hermes_cli/{update_cmd,update_lock,npm_engine,_startup_fast}.py`,
  `hermes_cli/web_routers/`, `hermes_cli/observability/`, `hermes_cli/subcommands/`,
  `model_setup_flows.py` — several now CARRY ported OTTO customizations (update-branch
  defaults, cmetech URLs, `_uv_subprocess_env`, brand version label, skills displayName,
  provider-reload) → CLAUDE.md surface rows need updating to the new paths.
- **Tool Search progressive disclosure** (`tools/tool_search.py`) — interacts with tool
  pinning; plugin-agent workers pin it off (see §4). Watch on future merges.
- `optional-skills/` tree (skills reorg; overlaps our curation excludes — fail-open, fine).
- `vercel_sandbox` environment (given our `clean=` kwarg integration), wake-word extra,
  root eslint toolchain, `--force-commit` installer flag, per-slot MoA `enabled`,
  `webhooks` desktop view, `apps/desktop/src/lib/version-status.ts` (now lightly OTTO-owned).

## 6. Follow-ups / deferred
1. **CLAUDE.md surface-table refresh** for the moved-code ports (update_cmd/_startup_fast/
   web_routers paths; `--no-package-lock` row superseded by upstream's `--no-save`;
   version-status.ts + plugin_agent_worker tool-search rows to add).
2. **otto-upstream-merge skill maintenance**: drop the stale `'Co-Worker'` emitter grep
   (design reversed 2026-07-15); update the `no-package-lock` grep; widen the release-notes
   bundle grep to `dist/assets/*.js` (code splitting); decide the `--diff` coverage-validation
   question (structurally unpassable — see trial report §pre-existing defects).
3. **Checker fix**: `python-isolation.yaml` fails against pristine base (`_exact_pattern`
   `$`-lookbehind vs PowerShell `$InheritedPythonEnvVars`) — checker regression, not merge.
4. **Paired branded-release rule**: when this delivery is approved for release, build/release
   BOTH brands at the same version per CLAUDE.md.
5. Housekeeping (pre-existing): `.superpowers/brainstorm/` pid/port state files committed on
   base; fork's `_check_via_local_git` banner path still counts against `origin/main`
   (pre-existing base behavior, review whether it should be brand-branch-aware).
6. Deferred smoke: sidebar resume behavior (session-states resolution adopted upstream's
   supersede — flagged in the trial report); a visual desktop chat send (app launched and
   healthy; backend chat path proven via CLI).
