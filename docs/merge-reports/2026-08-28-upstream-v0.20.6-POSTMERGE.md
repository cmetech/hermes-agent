# POSTMERGE report — upstream v0.20.6 → `base` → branded v7.1.0

## Outcome

The upstream v0.20.6 merge, branded branch regeneration, package/launch gates,
and paired stable v7.1.0 releases completed successfully. The live checkout
returned to `base`. No v7.1.0 product tag or release was created in the source
repository.

## Immutable lineage

- Previous verified upstream: `fcbd1076a93841fa88855acce810e342a5b78101`
  (`0.20.5`).
- Upstream target: `5fc308a70719a83cccdbba4c0e39c23f5a8239d5`
  (`0.20.6`).
- Real ancestry-preserving merge and tested release-time `base`:
  `6f6f8d88b0e2aeb061ac14e073a4bf5eea058b40`.
- OTTO v7.1.0 source: `8c877c423d3daf0770739e419541a1029f64ccf4`.
- LOOP24 v7.1.0 source: `8443a476e5b02c979805f8810346932c8cb80df5`.

The conflict, silent-overlap, importance, applicability, and disposition review
is preserved in `2026-08-28-upstream-v0.20.6-TRIAL.md`. The real merge has the
reviewed downstream parent and exact upstream target as its two parents and
reproduces the reviewed candidate tree.

Each branded source is a two-parent merge of its previous brand ref and the
exact tested release-time `base`. The generator then restored the descriptor-
owned overlay before the source commit was sealed.

## Findings carried into the real merge

| Importance | Finding | Durable result |
|---|---|---|
| High | Upstream pager defaults used plain `export` inside the hardened shell initialization. | The shared boundary uses `builtin export`; all 74 base-environment tests passed. |
| High | Two workflow MCP fixtures still imported the MCP 1.x `FastMCP` surface after the merged lock moved to MCP 2.0. | The fixtures use the MCP 2.0 `MCPServer` contract; the 170-test workflow MCP file and full workflow gate passed. |
| Medium | Desktop theme and local-pack tests froze neutral or OTTO identity values that generation legitimately changes. | Both assertions now test behavior derived from the active brand/package metadata. |
| Low | A POSIX managed-update fixture used Linux-only `/bin/true`. | The fixture uses portable `/usr/bin/true`; production launcher behavior is unchanged. |

No additional product defect appeared during real-commit brand regeneration,
packaging, fresh launch, or publication.

## Verification

### Neutral merge candidate

- All 17 merged-state customization ledgers passed: 369 total records, 364
  executable passes, five reference-only records, zero failures, and zero
  flaky-first attempts.
- Canonical base gate: 3,971 Python tests passed with eight platform skips
  across 75 files.
- Installed-distribution gate: three passed.
- Merge-gate Desktop matrix: 207 passed across 11 files.
- Targeted reconciliation suites: 586 Python tests passed.
- Base environment: 74 passed; workflow MCP: 170 passed; baked workflow seed:
  16 passed.
- Python compilation, PowerShell parsing, `uv lock --check`, index, whitespace,
  and exact-candidate evidence seals passed.

### Branded release candidates

- OTTO and LOOP24 each passed all eight brand-generator emitters and regenerated
  their package locks offline with zero reported vulnerabilities.
- OTTO passed 137 targeted Python/skin checks; LOOP24 passed 121 targeted
  Python checks. Both branded and legacy CLI entry points launched and reported
  their versions.
- For each brand, the complete Desktop gate passed:
  - renderer UI: 652 files and 6,520 tests;
  - Electron platform matrix: 133 files and 1,964 tests passed, with two files
    and six tests skipped by platform guards;
  - Electron `node:test`: 49 tests;
  - bundled Desktop plugins: 625 tests;
  - renderer, Electron, and E2E typechecks; production builds; native dependency
    staging; packaged-app validation; and artifact checks.
- ESLint passed with zero errors and 205 existing warnings.
- Both brands passed release-faithful macOS DMG packaging with
  `productVersion: 7.1.0`, the exact source commit/branch, the correct release
  repository, and a clean build stamp.
- Both packaged applications passed the isolated fresh-launch harness with a
  temporary user-data directory and `HERMES_HOME`.

## Release evidence

| Brand | Source | Workflow | Release | Assets |
|---|---|---|---|---:|
| OTTO | `8c877c423d3d…` | [successful run 33208189541](https://github.com/cmetech/otto/actions/runs/33208189541) | [v7.1.0](https://github.com/cmetech/otto/releases/tag/v7.1.0) | 7 |
| LOOP24 | `8443a476e5b0…` | [successful run 33208191984](https://github.com/cmetech/loop24/actions/runs/33208191984) | [v7.1.0](https://github.com/cmetech/loop24/releases/tag/v7.1.0) | 7 |

Both releases are stable, are neither draft nor prerelease, name the exact
branded source commit in their body, and contain the expected seven assets:
macOS arm64 DMG/ZIP plus blockmaps and Windows x64 EXE/MSI plus the EXE
blockmap. The external workflows were preflighted before dispatch and retained
Electron Builder's `--publish never` guard, leaving GitHub release publication
to the explicit release job.

## Remaining risks

- The v7.1.0 installers are unsigned, so Windows SmartScreen and macOS
  Gatekeeper warnings remain an expected distribution limitation.
- Darwin cannot execute Linux-only and Windows-only tests; the explicitly
  guarded skips remain covered by their platform CI lanes.
- GitHub annotated the release runs because several upstream actions still
  declare the deprecated Node 20 action runtime. GitHub forced them onto Node
  24 and all jobs passed; the warning should be cleared by updating those
  external workflow actions when supported versions are available.
- Release workflows live outside the source repository. Future releases must
  repeat the documented input and `--publish never` preflight.

## Catalog closure and next merge

All 176 entries across the 17 customization manifests now use
`5fc308a70719a83cccdbba4c0e39c23f5a8239d5` as
`last_verified_upstream`. The next upstream assessment must compare each
manifest from that immutable baseline to a newly requested release. It must
not re-review v0.20.0 through v0.20.6 or substitute moving `origin/main` for
the requested target.
