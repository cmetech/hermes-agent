# Workflow v3.0.3 Revision 7 verification and handoff

**Release contract:** `docs/superpowers/specs/2026-07-21-workflow-v3.0.3-design.md`

**Branch base:** `f1704cc0035f435275268b94df8189cb1551fa15`

**Historical plan-completion gate SHA:** `a6be8121ee3a0d27cbf3a68d0260dea3ed087458`

**First adversarial-remediation production SHA:** `51cd5c30226caad13cfc25f367bcd1b4ff71b41c`

**Final FV-RT-A runtime-remediation SHA:** `6e1dc254cba7`

**Final verified branch HEAD before this handoff update:** `70e4b1d8f7dfbdc3625f29b94b8a9e619164ccd6`

**Previous verification-handoff update SHA:** `12429cf5a325efab4bdf6dd7f227b480f019d538`

**Final FV-RT-A-1 recovery-convergence SHA:** `75e25d117822419d414c105cfb8c9ce8669f77c3`

This is verification evidence, not a release action. No branch was merged,
pushed, tagged, or published, and automated verification made no external or
billable inference request.

## Current disposition

**Branch merge status: READY TO MERGE.** Historical FV-RT-A review evidence
remains approved: the runtime remediation review returned **APPROVE, Critical
0 / Important 0 / Low 0**, and the subsequent customization-checker
implementation review returned **Critical 0 / Important 0 / Low 1** and
approved the implementation. Its Low concerns report wording only: the exact
progress-ledger exemption applies to both ordinary and commit-scoped diff
classification. That wording finding is recorded in the ignored progress
ledger and does not change the implementation verdict. The final FV-RT-A-1
follow-up is also closed: Task 13 review returned **spec Pass, task quality
Approved, Critical 0 / Important 0 / Low 0**.

**Release readiness: BLOCKED.** All automated merge evidence is green, but the
seven configured-Gateway/Electron manual-only gates listed below remain
**OUTSTANDING**. They are the only remaining release blockers.

## Final FV-RT-A-1 recovery-convergence follow-up

The final follow-up at `75e25d117822419d414c105cfb8c9ce8669f77c3` closes
FV-RT-A-1. It is distinct from the FV-RT-A repair-isolation remediation
documented below: it proves recovery convergence after a repairable projection
failure rather than relabeling the earlier isolation evidence as final.

- Strict RED: after four later sweeps, an unattended v13→v14 run never
  submitted; direct first-touch `load_run` raised `run schedule index parity
  mismatch`. Two additional corruption subclasses also failed before the
  fallback boundary was widened.
- Focused GREEN: **4/4**. Valid rewritten, malformed/non-JSON, and wrong-run
  projection bytes recover from the intact authoritative journal; the exact
  canonical SQLite `scheduled_at` is restored; later coordinator submission
  needs no out-of-band reads; and direct first-touch `load_run` reconstructs
  without a schedule-parity failure.
- The six-file recovery matrix passed **152/152**. Task 13 review returned
  **spec Pass, task quality Approved, Critical 0 / Important 0 / Low 0**.
- The shared final matrix passed **595/595 across 23 files in one clean
  attempt**. Its original Strategy A 22-file subtotal is **556/556**, with the
  coordinator contributing **39**.
- Installed-distribution integration passed **1/1**; Desktop Workflow UI
  passed **144/144 across 16 files**; renderer and Electron TypeScript
  compilation passed; and the merge-gate inventory passed **26/26**.
- Complete customization-checker tests passed **9/9** and the full-history
  live customization checker exited **0**. Scoped Ruff, full-range and HEAD
  diff checks passed. `brands/` and `plugins/model-providers/` remain
  unchanged from branch base, and the protected Revision 6 and Revision 7
  review-request hashes remain `ca8335b0...` and `9204adda...`.

FV-RT-A-1 is closed and is not part of the remaining deferral set. The
deliberate deferrals remain in
`docs/backlog/v3.0.3-release-review-deferrals.md`; they do not widen the seven
manual-only release gates below.

## Final FV-RT-A remediation automated gates

The following evidence was collected for the final runtime remediation at
`6e1dc254cba7` and the verified branch state at
`70e4b1d8f7dfbdc3625f29b94b8a9e619164ccd6`:

- The first 23-file matrix attempt passed **590** tests and failed **1**:
  `test_gateway_leader_and_web_standby_report_cached_health` missed its
  two-second deadline under 56-worker load.
- The exact failed test then passed **1/1 in 0.25 seconds**, and the complete
  coordinator test file passed **39/39 in 35.77 seconds**. The remediation
  range did not change the failing startup-health assertion.
- An identical clean 23-file rerun passed **591/591**. Within that result, the
  original Strategy A 22-file subtotal is **552/552** and the coordinator file
  contributes the additional 39 tests.
- Installed-distribution integration passed **1/1**.
- Desktop Workflow UI passed **144/144 across 16 files**.
- Renderer and Electron TypeScript compilation passed.
- The merge-gate inventory within the matrix passed **26/26**.
- The complete customization-checker test file passed **9/9**, and the live
  checker against committed HEAD exited **0**.
- Ruff passed across every Python file changed since `a23a5fa4f`.
- The full-range diff check and HEAD commit check passed.
- `brands/` and `plugins/model-providers/` remain unchanged from the branch
  base.
- The protected Revision 6 and Revision 7 review-request SHA-256 values remain
  `ca8335b0...` and `9204adda...`.

The historical 1,045-test base gate described below was **not rerun** at the
final runtime-remediation SHA. It remains historical plan-completion evidence
and is not included in the 591-test final matrix.

### Earlier adversarial-remediation evidence

Before FV-RT-A was identified, automated evidence at production SHA
`51cd5c30226caad13cfc25f367bcd1b4ff71b41c` established the earlier
adversarial remediation:

- The exact Strategy A 22-file matrix passed **536/536** on its clean full
  rerun. The first full attempt encountered one unchanged timing-sensitive
  failure in `test_parallel_scheduler.py`; the complete file immediately
  passed **19/19** in isolation, and the identical full matrix then passed
  536/536. No production scheduler behavior changed in that remediation.
- Installed-distribution integration passed **1/1**.
- Desktop Workflow UI passed **144/144 across 16 files**.
- Renderer and Electron TypeScript compilation passed.
- The explicit merge-gate inventory passed **26/26**.
- Scoped Ruff, the upstream-customization checker, range and staged diff
  checks, and commit hygiene passed.
- `brands/` and `plugins/model-providers/` remained unchanged across that
  remediation range.
- The whole-branch review at that point returned **APPROVE**, with Critical 0
  and Important 0. Its documentation-only Minor was closed by the prior
  verification refresh.

No mock substitutes for these exact operator-owned release gates:

| Manual-only release gate | Status |
| --- | --- |
| Configured-Gateway structured `tool_calls` round trip for OTTO | **OUTSTANDING** |
| Configured-Gateway structured `tool_calls` round trip for LOOP24 | **OUTSTANDING** |
| Electron laptop-diagnostic approval and rejection | **OUTSTANDING** |
| Electron ai-extensions through the configured Gateway to success | **OUTSTANDING** |
| Electron Run later → Scheduled → due success | **OUTSTANDING** |
| Electron cancel-before-fire | **OUTSTANDING** |
| Electron approval-gate regression | **OUTSTANDING** |

## Historical plan-completion gates and baseline reconciliation

At historical plan-completion gate SHA
`a6be8121ee3a0d27cbf3a68d0260dea3ed087458`:

- `scripts/test_workflow_merge_gate.sh --phase base`: 1,045 Python passed,
  1 skipped; installed-distribution 1/1; Desktop merge selection 109/109
  across 11 files; renderer TypeScript passed; exact
  `TESTED_BASE_SHA=a6be8121ee3a0d27cbf3a68d0260dea3ed087458`.
- `npm run test:workflow-ui`: 144/144 across the prescribed 16 files.
- `npm run typecheck`: renderer and Electron TypeScript projects passed.
- scoped Workflow ESLint: zero errors and 17 pre-existing
  `padding-line-between-statements` warnings.
- Ruff lint over every Python file changed from the branch base passed.
- customization checker, `git diff --check`, and commit checks passed.
- the 22-file contract matrix passed 524/524, including the limit/executor and
  mixed-concurrency suites that are explicitly outside the focused base gate.
- `skills/gateway-toolcall-parity/selftest.py` completed `SELFTEST OK` against
  loopback mock gateways, including structured Anthropic, OpenAI, and Ollama
  tool-call and tool-execution round trips.

Against Task 0's 775 Python passed/1 skipped, installed-distribution 1/1,
Desktop 90/90 across 11 files, and green TypeScript baseline:

| Boundary | Baseline | Final | Delta | Reconciliation |
| --- | ---: | ---: | ---: | --- |
| Python | 775 | 1,045 | +270 | Workstream 1 +14 (789), Workstream 2 +52 (841), Workstream 3 +88 (929), Workstream 4 +116 (1,045). The exact gate-inventory test proves every workflow test is selected or explicitly accounted for and no selected scheduling suite was removed. |
| Python skips | 1 | 1 | 0 | The existing platform/environment skip remains the sole skip. |
| Installed distribution | 1 | 1 | 0 | The existing wheel extraction test was strengthened without adding a second case. |
| Desktop merge selection | 90 | 109 | +19 | Workstream 1 +1 (91), Workstream 2 +2 (93), Workstream 3 +1 (94), Workstream 4 +15 (109); the file count remains 11. |
| TypeScript | pass | pass | — | Both renderer and Electron projects compile. |

The test additions are behavior contracts rather than mutable catalog/version
snapshots. Task reports and the progress ledger retain each task's individual
RED/GREEN delta.

## Golden, migration, and recovery evidence

- `test_v302_rest_and_cli_absence_fixtures_derive_without_mutation` pins both
  historical entitlement shapes and their exact start digests.
- `test_v302_same_key_resubmission_joins_existing_without_metadata_upgrade`
  covers both REST and CLI fixtures. The fixture file SHA-256 is
  `9fde41b700b7d9c0b0904b3ba22f11dfb3f4712628683acf50acda37fe446207`.
- `test_unscheduled_api_golden_digest_and_metadata_remain_byte_identical`
  pins the ordinary unscheduled API digest.
- `test_complete_one_shot_scheduling_contract_is_selected_in_release_gates`
  proves `test_schedule_store_identity.py`, `test_scheduled_runs.py`,
  `test_schedule_revalidation.py`, and
  `test_scheduling_middleware_e2e.py` appear exactly once in both the base
  and native three-OS gates with no opt-out.
- `test_every_reconstruction_path_preserves_scheduled_identity`, the genuine
  v13 migration/refusal cases, namespace reconstruction, damaged-index
  repair, and journal-only recovery all pass.

## Mandatory named-coverage mapping

| Revision 7 coverage item | Passing evidence |
| --- | --- |
| Real worker MCP startup, tool-schema delivery, PID reap, and cleanup on every early exit | `test_real_plugin_agent_runs_request_mcp_and_reaps_it`; `test_request_mcp_cleanup_covers_every_post_start_failure`; `test_local_stdio_mcp_is_process_isolated_and_reaped_after_shutdown` |
| Failed required server and `codex_app_server` produce zero provider calls | `test_failed_request_mcp_is_typed_and_never_reaches_provider`; `test_runtime_failure_precedes_request_mcp_start_and_provider`; `test_incapable_runtime_fails_before_agent_construction_and_mcp_start` |
| Prospective/actual runtime-mode parity | `test_prospective_classifier_matches_actual_effective_runtime`; `test_bedrock_classification_is_fresh_pure_managed_and_matches_resolved` |
| Generated brand HTTP-loop classification and structured gateway tool calls | paired-brand provider proof plus loopback `gateway-toolcall-parity/selftest.py` |
| API/coordinator production-binding invariant; no request seam | `test_coordinator_uses_the_same_production_binding_and_request_has_no_seam`; `test_production_api_and_coordinator_share_binding_declaration_and_no_request_seam` |
| One raw cache, capable/incapable context projections, matching distinct risk | `test_verified_showcase_cache_contains_authenticated_raw_material_only`; `test_same_raw_cache_recomputes_capable_and_incapable_showcase_projections` |
| Exact `RunExecutionLimits`, negative `max_iterations`, every mapping | `test_run_execution_limits_is_frozen_and_contains_only_per_run_fields`; `test_run_execution_limits_resolve_every_field_by_tightening_profile`; `test_ai_request_maps_every_run_execution_limit_exactly`; `test_approval_rework_request_maps_every_run_execution_limit_exactly`; `test_scheduler_threads_limits_from_sealed_snapshot_to_process_boundary` |
| Mixed-run and global concurrency composition | `test_mixed_run_limits_share_profile_pool_without_leaking`; `test_store_composes_per_run_worker_ceiling_with_profile_ceiling` |
| v3.0.2 REST and CLI entitlement shapes, adoption, deterministic rejection | `test_v302_rest_and_cli_absence_fixtures_derive_without_mutation`; `test_v302_same_key_resubmission_joins_existing_without_metadata_upgrade`; coordinator adoption coverage in `test_coordinator.py`; `test_desktop_rejects_laptop_showcase_without_real_ai_under_offline_mode` |
| Non-AI explicit-real failure and every deterministic runner consumer | `test_explicit_real_requires_exact_verified_ai_corroboration`; command/prompt, loop, inline-agent, and approval-rework tests in `test_ai_entitlement.py` |
| Legacy flat REST values unchanged | `test_post_runs_legacy_flat_workflow_still_accepts_arguments`; `test_post_runs_api_admission_joins_identical_and_conflicts_on_changed_input` |
| Authenticated translation, declared-input errors, read-once fixtures, no raw projection, manifest identity | the seven authenticated-input cases in `test_desktop_api.py`, plus both laptop middleware E2Es |
| CLI has no AI token but stays incompatibility-honest | `test_ai_preflight_has_no_confirmation_gate`; `test_cli_run_rejects_ai_showcase_before_execution_side_effects` |
| Schema 13→14, newer-version refusal, projection-only reconstruction everywhere | migration/refusal and every reconstruction/parity case in `test_schedule_store_identity.py` |
| Real wake deferral, lane/quota/queue matrix, bounded wall-clock jumps | `test_real_pending_wake_completes_not_due_then_indexed_sweep_submits_once`; the future-consumer, due-policy, atomic-promotion, and five-second clock cases in `test_scheduled_runs.py` |
| Fire-time revocation, replacement, bundle/risk/capability failures | revoked trust, restart trust, source change, actual context change, forced showcase, sealed snapshot, and package-preparation cases in `test_schedule_revalidation.py` |
| Cancellation retention and Desktop scheduling E2E | `test_authenticated_cancel_before_fire_retains_evidence_and_never_executes`; `workflow-operations.e2e.test.tsx`; `review-run-dialog.test.tsx` |
| Explicit Task 4.7 exactly-once proof | `test_authenticated_run_later_defers_real_wake_then_executes_checkpoint_once` asserts one completed `run_admitted` wake plus revalidation projection/promotion; `test_restart_and_index_reconstruction_preserve_schedule_and_exactly_once` asserts one terminal `run_succeeded` after repeated sweep |

## Failure-table mapping

| Failure-table condition | Passing test(s) |
| --- | --- |
| AI showcase incompatible in list/detail | `test_same_raw_cache_recomputes_capable_and_incapable_showcase_projections` |
| Same incompatibility at admission | `test_showcase_admission_uses_server_binding_and_incapable_is_zero_residue` |
| Immediate actual runtime changes or is externally owned | `test_capable_admission_then_actual_app_server_runtime_fails_before_provider` |
| Required request MCP cannot start | `test_failed_request_mcp_is_typed_and_never_reaches_provider` |
| Scheduled runner/runtime capability changes | `test_scheduled_ai_revalidates_actual_runner_and_runtime_before_claim` |
| Non-AI scenario declares an agent-backed feature | `test_copied_non_ai_bundle_rejects_agent_backed_features` |
| Deterministic entitlement reaches any agent consumer | command/prompt, loop, inline-agent, and approval-rework cases in `test_ai_entitlement.py` |
| Legacy REST/CLI showcase has no explicit entitlement | `test_v302_rest_and_cli_absence_fixtures_derive_without_mutation` |
| v3.0.2 same-key resubmission | `test_v302_same_key_resubmission_joins_existing_without_metadata_upgrade` |
| Explicit real is non-AI or mismatched | `test_explicit_real_requires_exact_verified_ai_corroboration`; both mounted integrity E2Es |
| Unknown/malformed entitlement | `test_absent_ordinary_run_is_real_but_malformed_value_is_deterministic` |
| Declared input missing, undeclared, or reserved | `test_post_runs_declared_inputs_return_typed_errors_without_residue`; `test_post_runs_refuses_authenticated_internal_or_fixture_owned_names` |
| Legacy flat values | `test_post_runs_legacy_flat_workflow_still_accepts_arguments` |
| Per-value or aggregate byte cap | `test_post_runs_relocates_only_byte_caps_to_typed_endpoint_validation` |
| Unrelated request schema error | the ordinary-validation branch of `test_post_runs_relocates_only_byte_caps_to_typed_endpoint_validation` |
| Fixture escapes, aliases, or is a symlink | authenticated fixture path/mapping cases in `test_showcase_catalog.py` |
| Fixture mutates after verification | `test_post_runs_uses_once_verified_fixture_bytes_after_source_mutation` |
| Raw input projects or logs | `test_post_runs_changed_authenticated_fixture_identity_conflicts_without_raw_data`; laptop middleware evidence-canary scans |
| Scheduling showcase omits `schedule_at` | `test_post_runs_rederives_showcase_run_support_without_persistence` |
| `schedule_at` malformed, naive, or non-future | `test_post_runs_rejects_invalid_schedule_at_before_store_or_package_work` |
| Future real admission wake | `test_authenticated_run_later_defers_real_wake_then_executes_checkpoint_once` |
| Future same-key run affects immediate lane | `test_future_scheduled_run_never_blocks_same_key_immediate_work` |
| Due `forbid` overlaps active work | `test_due_scheduled_run_preserves_declared_overlap_policy_before_claim` |
| Fire-time trust/source/bundle/risk changes | corresponding user/showcase cases in `test_schedule_revalidation.py` |
| Cancel before fire | `test_authenticated_cancel_before_fire_retains_evidence_and_never_executes` |
| SQL/projection schedule parity loss | SQL-only and projection-only corruption cases in `test_schedule_store_identity.py` |
| CLI AI scenario | `test_cli_run_rejects_ai_showcase_before_execution_side_effects` |
| CLI scheduling tour | `test_scheduling_requires_confirmation_and_preserves_unrelated_jobs` |

## Contract audit and carried findings

The passing matrix directly audits context-free cache contents,
compatibility-before-risk recomputation, runner plus actual runtime
composition, complete isolated-worker MCP lifecycle, immutable production
binding, both legacy entitlement shapes, legacy flat values, authenticated
input translation and redaction, the exact limits record, real wake handling,
every future-queued consumer, projection-only schedule reconstruction,
promotion-bound fire-time authorization, cancellation retention, and unchanged
Cron optional-MCP behavior.

Task 1.2's Low is closed by exact INFO and WARNING assertions containing only
the exception class. Task 4.7's Low is closed by explicit singleton wake,
revalidation, promotion, and terminal-success assertions. The Task 2.1 stale
design sentence is corrected; the authenticated orphan prompt remains dormant
only to preserve the v3.0.2 package tree and start identities. Task 3.4's
historical same-commit manifest procedure cannot be changed without rewriting
reviewed history; the final manifest is semantically current and its checker is
green.

## Paired-brand and provider evidence

Detached no-ref worktrees at
`65b407ae1bd249d9afaf675452d62105da24ef0f` ran generator `--write`, explicit
`--check`, local provider proof, gateway parity selftest, and the brand gate:

- OTTO: 8/8 emitters; `api_mode=chat_completions`;
  `hermes_managed_tool_loop=True`; loopback
  `http://127.0.0.1:18080/v1`; `SELFTEST OK`;
  `TESTED_BRAND_SHA=65b407ae1bd249d9afaf675452d62105da24ef0f`.
- LOOP24: 8/8 emitters; `api_mode=chat_completions`;
  `hermes_managed_tool_loop=True`; the same Hermes-managed loopback Gateway;
  `SELFTEST OK`;
  `TESTED_BRAND_SHA=65b407ae1bd249d9afaf675452d62105da24ef0f`.

Both detached worktrees were removed and no branch ref was created or updated.
Because recording this evidence creates a documentation-only descendant, the
Task 5 report records the required repeat at the final tracked handoff SHA.
No generated brand change remains in the feature worktree.

## Manual-only evidence status

No already-authorized configured real Gateway/Electron environment was made
available to this automated implementation session. The following remain
manual-only release blockers; no mock is represented as their evidence:

- one configured-Gateway structured `tool_calls` round trip for OTTO and one
  for LOOP24;
- Electron laptop-diagnostic approval and rejection;
- Electron ai-extensions through the configured Gateway to success;
- Electron Run later to Scheduled and due success, plus cancel-before-fire;
  and
- Electron approval-gate regression.

The automated loopback provider, real isolated worker, middleware, scheduler,
and gateway-parity self-tests are complementary contract evidence, not a
substitute for those operator checks.
