# Declarative reference scanner: complete-branch adversarial review

Status: complete. Technically ready for the requested feature-branch commit, with the repository-wide baseline limitations recorded below. This is the single continuous complete-branch review, including the bounded merge-gate correction; it is not another architecture or plan review.

## Scope and method

Reviewed the intended uncommitted feature against starting commit `74fed08f91014ca7fc80ee9ea4427568ec95b04f` in `.worktrees/workflow-reference-scanner-contract`, branch `feat/workflow-reference-scanner-contract`. HEAD remains the starting commit under the required review-before-commit delivery sequence.

The review covered the complete production and test changes, all 184 literal scanner/substitution/path cases, their manifest including workflow rows, the historical preservation test and digest manifest, the replacement specification and implementation plan, and the focused task finding/disposition records supplied in `final-branch-review-package.md`. Large historical JSON captures were reviewed through their exact indexed hashes, preservation assertions, and already verified provenance rather than redundantly dumped. The package was read in bounded passes; truncated portions were recovered explicitly, and the literal JSON and manifest were read in compact form without deriving expected results.

Two named compatibility risks warranted limited unchanged-source reads: whether renderer masking, eager parsing, and per-template output resolution matched the published caller policies (`plugins/workflow/resources.py:1021`, `:1074`, `:1198`), and whether quoted heredoc bodies were being incorrectly classified as literal (`plugins/workflow/bash_rendering.py:585`, `:631`). Both agree with the amendment. Existing include/scheduler test locations were checked for the separately retained v4/v3 continuity witnesses. The final Task 6 verification report and bounded raw evidence were then reviewed, including exact failed-node set comparison, final wrapper summaries, the independently initialized Git baseline result, the resource-exhaustion reproduction, and runtime-diff/static-check output. No reported suite was rerun, no expectation was generated by executing a scanner, and no source, test, index, branch, worktree, or Studio state was changed by this review. This report is the only reviewer-written file.

## Strengths

- Historical isolation is explicit. Reader 3 and expanded trusted limits are selected only for Archon v6 (`plugins/workflow/language_schema.py:4605`); seven historical contracts and the exact legacy corpus remain byte-checked. The old v6 comparison permits only the approved publication delta, while definition/companion schemas and node kinds remain equal (`tests/plugins/workflow/test_reference_scanner_baselines.py:109`).
- The 38-field capability projection comes from the existing inventory and retains scope applicability, authenticated-body sources, the inline-script discriminator, grouped ordered leaves, and container-major traversal (`plugins/workflow/language_schema.py:2559`). The former scoped view is retained, and only v6 root strict-reference paths acquire the complete root projection. The new inventory annotations do not alter runtime traversal.
- Metadata preserves meaningful caller distinctions: text versus Bash candidate boundaries, conditional marker rules, complete-reference presence, direct previous-reference API versions versus workflow activation, condition iterator versus validators, previous masking, scalar overlap, and branch-local diagnostic mappings (`plugins/workflow/reference_scanner_contract.py:158`, `:318`). Tests execute every published Bash example rather than trusting its label alone (`tests/plugins/workflow/test_reference_scanner_contract.py:432`).
- Literal coverage is substantive. It includes all named malformed suffixes; first/drain and prefix-before-error behavior; invalid versions and span shapes; escaped/comment/doubled-dollar versus live Bash candidates; substitution, arithmetic, conditional, extglob, brace, array, declaration, function, coprocess, case, redirection, and heredoc families; 64/65 nesting; context-before-grammar errors; adjacent previous/current/scalar references; and distinct values for all ten scalars. Static schema proofs and immutable runtime output resolution are separate families, including retained reference constraints and both numeric interpretations.
- The fixed test adapters preserve lazy prefixes and eager atomicity, serialize native errors and meaningful causes, call authenticated-resource APIs with real temporary packages, and leave invalid-byte decoding to runtime code (`tests/plugins/workflow/reference_scanner_observations.py:64`, `:80`, `:94`, `:123`). Case data cannot supply executable control flow. Publication reads literal data and computes the corpus digest without executing these adapters or scanners (`plugins/workflow/language_conformance.py:1598`).
- Workflow cases establish Phase-4 root diagnostics, container order, body/current/outer/previous scope interactions, multigroup isolation, and competing-error order. Versioned CLI structure/count checks precede serialization; canonical and emitted bounds precede stdout (`plugins/workflow/schema_cli.py:151`). Generic emitter behavior and legacy publication are preserved.
- Packaging proof builds and inspects direct wheel, sdist, and sdist-built wheel, then runs both installed wheels outside the checkout with no `PYTHONPATH`, verified installed resource origins, demonstrated socket guards, and source-output equality for both profiles/publications/output modes (`tests/plugins/workflow/test_installed_distribution_e2e.py:427`, `:662`). The README makes reader/profile rejection and separate Studio delivery explicit.

## Issues

### Critical

None found in the reviewed branch changes.

### Important

**P2 — New scanner suites were omitted from the existing workflow merge gate (addressed).** `scripts/test_workflow_merge_gate.sh:411` selects the existing language suites but omits `test_reference_scanner_baselines.py`, `test_reference_scanner_contract.py`, and `test_reference_scanner_conformance.py`. The inventory assertion at `tests/scripts/test_workflow_merge_gate.py:317` requires every workflow test file to be selected or explicitly opted out; the controller reproduced these three omissions during repository verification. This causes a real feature-related gate failure and leaves the new compatibility proofs outside the established gate. Register the three files in the existing selection, without opting them out or weakening the assertion. **Disposition: addressed.** The scoped final correction adds exactly those three paths at `scripts/test_workflow_merge_gate.sh:412`, preserving the existing base-phase list, options, and shell continuation style. The supplied correction package changes no runtime, corpus, test assertions, or opt-outs. The existing inventory test failed before the edit and passed afterward; the complete affected gate file then passed all 51 tests, and shell syntax plus diff checks passed. Reviewed this exact correction within the same continuous branch review; no new issue was found and no affected suite was redundantly rerun.

### Minor

None requiring a branch change. The focused task findings are resolved in the reviewed implementation; they are not reopened here.

## Requirements assessment

| Requirements | Assessment |
|---|---|
| H1 | Seven immutable authoring pairs, exact legacy corpus, and narrowly bounded v6 characterization delta are covered by the reviewed preservation tests and reported focused pass. |
| S1, S2, H2 | Required activation metadata, grammar/boundaries, API/version distinctions, scalars, laziness, masking, and cause observations are present. Later Studio rejection behavior remains separate. |
| S3 | Required Bash state families and interactions are represented in metadata and reviewed literal cases, supplemented by the unchanged Bash suites. This is finite compatibility evidence, not a proof over every input. |
| S4 | Static/runtime path families preserve containing and referenced constraints, union behavior, numeric key/index alternatives, and conservative unknowns. |
| S5, S6, S7 | Complete inventory projection, grouped traversal, root Phase-4 fields, scope diagnostics, shadowing, previous-output policy, and ordering have metadata, literal, and workflow evidence. Authenticated-body checks use their real APIs. |
| S8 | Hermes supplies multigroup/multisurface compatibility inputs. Studio indexing and reuse are deferred. |
| S9 | Reported workflow verification includes the unchanged operation-count/performance tests. Studio 250-node/500-edge canvas and pointer-frame acceptance are deferred; this review makes no Studio performance claim. |
| S10 | Explicit Python/Unicode profiles, profile-sensitive literals, astral offsets, and separate direct-API surrogate characterization are covered. Matrix execution is reported passed; Studio UTF-16 boundary behavior is deferred. |
| H3 | Expectations are static literals with a separate evidence manifest. Publication cannot calculate expectations from runtime behavior; the corpus binds the contract and its own self-excluding canonical digest. Authoring chronology rests on task records, not a claim that the final diff proves chronology. |
| H4 | Reported integration passes execute the reviewed installed-artifact/offline proof. |
| H5 | Runtime source changes are excluded by the reviewed diff and final static/AST audit. The full workflow suite passes apart from host skips. The full repository was run; all 103 remaining failed node IDs and the separate resource-exhaustion condition reproduce at the starting commit. The feature-related merge-gate omission is fixed. |

## Verification evidence

Reviewed the completed [verification record](2026-09-04-declarative-reference-scanner-verification.md), the final [delivery record](2026-09-04-declarative-reference-scanner-delivery.md), and bounded raw evidence under `.superpowers/sdd/2026-09-04-declarative-reference-scanner/`. All pytest execution used `scripts/run_tests.sh`; these are verifier executions, not reviewer reruns.

| Verification | Final result |
|---|---|
| Focused publication/baseline tests | 506 passed, no failures, skips, or flakes reported |
| Explicit supported-Python matrix | 1 passed across CPython 3.11.16/Unicode 14.0.0, 3.12.14/Unicode 15.0.0, and 3.13.15/Unicode 15.1.0 |
| Explicit installed integration tests | 2 passed, no failures, skips, or flakes reported |
| Full workflow suite | Exit 0; 136 files, 6,305 passed, 44 host skips, no failures or flakes reported |
| Corrected workflow merge-gate file | Exit 0; 51 passed; shell syntax and diff checks passed |
| Full repository suite after declared prerequisite repair | Exit 1; 3,750 files, 51,850 passed, 103 failed, 657 skipped in 730.3 seconds with 9 workers; one additional resource-exhaustion file lacked normal wrapper counts |
| Runtime/static continuity | `git diff --check` passed; the four designated runtime files have a zero-byte diff; the language-schema AST audit confirms scanner/parser/candidate bodies are unchanged |

The full repository suite is **not green**. Its 103 remaining failures span 32 unchanged test files. The verifier reran those files against an exact starting-commit source archive under the same interpreter and canonical wrapper. I independently compared the captured node-ID sets: 103 current failures, 113 baseline failures, an intersection of all 103 current failures, and zero current-only nodes. The extra archive failures belong to update tests affected by missing Git metadata. A further baseline copy with its own independent Git repository reproduced all 13 current update failures; the verifier compared their control-flow/mock-command causes rather than relying only on file-level overlap. The report classifies the other matching baseline causes, including host prerequisites, stale expectations/mocks, existing skill metadata, and existing source-policy assertions.

`tests/gateway/test_api_server_runs.py` is a separate limitation: pytest reaches partial results before exhausting file descriptors and raising `OSError: [Errno 24] Too many open files` during cleanup. The wrapper therefore cannot provide a normal test count for that file. I checked the same condition in the current standalone reproduction and starting-commit raw log; it is not silently counted as a pass or normal skip. This supports baseline/environment classification of the unresolved repository failures, while retaining the explicit red full-suite result.

Final artifact measurements fit the approved limits. The amended contract is 315,756 canonical bytes, leaving 8,244 usable bytes after the reserve; its scanner section is 31,879 bytes, leaving 121 bytes under the 32,000-byte bound. The Archon corpus has 54 workflow, 132 scanner, 23 substitution, and 29 structured-path cases, occupying 200,496 canonical and 255,731 pretty bytes. The seven historical authoring pairs and the exact 7,265-byte legacy corpus still match their recorded bytes. The final bound/digest evidence identifies:

- Archon contract digest: `sha256:02830acc6e3c797cec14e7debc3b2d15f91eca514bb9d2ffe31bd3b74e2549ed`.
- Archon corpus digest: `sha256:df14cc2dc8adec213e98a126360943b9b831426ef43304632a7582288ecd6384`.

The scanner section has little growth headroom, but it satisfies the approved bound; this is not an open defect or a reason to increase the limit. Exact remaining artifact hashes, all 103 failure IDs, and per-file cause classifications are preserved in the linked verification and delivery records.

## Recommendations

Preserve the red full-repository result and baseline comparison in delivery evidence. Keep Studio indexing, canvas performance, and UTF-16-boundary acceptance explicitly deferred. No further feature code change is requested by this review.

## Assessment

**Technically ready for the requested feature-branch commit: yes.** The single Important integration finding is addressed, the final scoped correction is reviewed, required Hermes-specific verification passes, and no feature-only failure remains in the completed repository comparison. The complete repository suite remains red for the independently reproduced baseline/environment conditions described above.

This is a technical review verdict, not authorization to merge into `base`, push, or start Workflow Studio work. Those actions remain behind the user's separate approval boundary.
