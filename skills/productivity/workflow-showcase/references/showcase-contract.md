# Showcase contract

The catalog and package bytes are authenticated by bundled digests before use. A run uses the ordinary workflow loader, immutable input snapshot, digest trust, idempotent admission, scheduler, RunStore, approval, artifact, and reporting contracts. Immutable run metadata identifies showcase ownership. Evidence refs are opaque and reports never expose source input paths, prompts, reasoning, credentials, or unrestricted arguments.
