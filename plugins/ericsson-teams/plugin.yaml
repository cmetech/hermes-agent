name: ericsson-teams
version: 1.0.0
description: "Microsoft Teams channel tools via Graph (MSAL device-code auth) — list teams/channels, read/send/reply."
author: Ericsson (cmetech)
kind: backend
provides_tools: [teams_auth, teams_list, teams_channels, teams_read, teams_send, teams_reply]
optional_env:
  - ERICSSON_GRAPH_CLIENT_ID
