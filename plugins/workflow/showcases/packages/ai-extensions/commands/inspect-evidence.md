---
description: Inspect bundled fictional evidence through bounded extensions
---
Use the resolved `ascii-art` skill, local echo MCP, mapped hooks, and one bounded
inline reviewer. Return only the declared JSON schema. Do not use the network or
include credentials.
